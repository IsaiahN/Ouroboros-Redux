# BUILD PLAN — The New Horse (autonomous build)

## Charter
Isaiah handed full control on 2026-07-20. The mission: **build the agent `docs/THE_MAP.md`
describes — one that solves ARC-AGI-3 by its OWN reasoning.** The corpus documents are the
doctrine (they substitute for Isaiah's judgment); THE_MAP is the spec; the FMap grounds each
design decision. Final submission deadline: **2 November 2026**.

## Operating discipline — non-negotiable (LAW 0 + THE MAP §VIII)
- **Never encode the answer.** The agent reasons its own way to it; I build the capability, never the solution.
- **Never ship half a mechanism.** A record without a consumer, an enumerate without a use, is worth nothing.
- **Nothing happens silently** — no isolated code, no silent code, no code without reason.
- **RESET earned-only; never downsample the grid** — enforced at import (hard bounds, not dials).
- **The brake:** a claim's strength never exceeds its gate. Every brick is unit-tested; the live game is the real gate.
- **RULE 0.7 — don't spiral, don't force a solve.** When stuck: abstract up, re-read the docs, query the FMap. A single run is never a verdict.
- **RULE 0 OF EVIDENCE — SEE BEFORE YOU SAY (ENFORCED GATE, not a guideline).** No claim about what a game IS or DOES, and no diagnosis of a run, may be written from TELEMETRY alone (pe_integral, belief, changed-cell counts, outcome). Before any such claim I MUST (a) render the actual recorded frames to images (`python tools/render_run.py <recording|scorecard_dir>`), (b) READ those images, and (c) base the diagnosis on what the PIXELS show. Telemetry only POINTS; the frames DECIDE. Every live-run log entry must cite what was SEEN in the frames. Diagnosing from numbers without rendering is a LAW-0-class violation. (This rule exists because ls20 run-2 was mis-diagnosed as "large/global change" from pe_integral when the frames showed a small maroon+lime piece moving in a maze — the numbers lied, the pixels didn't.)
- **Monolith-then-carve** — build a small COMPLETE loop, then refine. Do not gold-plate isolated parts or gate the design.
- **FMap-ground every design decision** — reason the problem's F* coords, rank the 46 frameworks, research the top one's blueprint + FAILURE MODES, build the guards in. (`/tmp/nb/fmap_query.py`.)
- **Don't build a decorated copy** — the map's one warning. Coherence is not correctness; only measurement is.

## Build sequence (adaptive — reorder as the build teaches)
Each brick: FMap-grounded, unit-tested, committed, nothing silent.
1. **[DONE]** Falsified ledger (§9.4) — the reject-memory Redux can't hold. Immune-system failure-mode audited. 10/10 tests.
2. **Full-resolution perception** (§9.2) — object permanence (identity by overlap, survives recolour/reshape), segmentation-as-revisable-belief, tracking through occlusion, `check_no_downsampling()` at import. Foundational: everything reads from it.
3. **Typed grammar** (§9.1) — primes (Kant/NSM), molecules, predicates × quantifiers. Adapt Redux's `objective_grammar.py` (already sound), cleaned and TYPED (typing beats size).
4. **The residual** (§8) — prediction error as the aim, the sense organ, and the currency. Full-resolution, un-suppressible (the brake invariant: no drive may suppress the time-integral of prediction error).
5. **The marketplace** (§9.3) — propose → vote → resolve, priced by prediction error. *FMap says: predictive_processing (hypotheses ranked by prediction error) + goodharts_law GUARD (the price must not become a gameable target).*
6. **Two streams + pose + α** (§9.2, I2a) — Stream A private residual history, Stream B the grammar, α the learnable weighting. Falsifier: divergent encounter order ⇒ divergent α.
7. **Loop closure** (§9.6) — PERCEIVE → THINK → MAP → ACT with both feedback arrows.
8. **Hard-bound enforcers at import** — reset-earned-only, no-downsampling.
9. **Run vs one clean ARC-3 game through the live gate.** Metric: `levels_completed` only (no fractional goose chase).
10. **[DONE]** Exploration drive + self-locus controllable-ID.

## HARVEST-DRIVEN SEQUENCE (bricks 11+) — see claude/HARVEST_from_redux.md
The ground-up bricks 1–10 are underpowered; the live runs kept rediscovering (painfully) what Redux already measured. Five subagent surveys of Redux (branch `main`) produced a harvest plan. Each item below is a Redux-PROVEN mechanism re-expressed **reset-free, map-faithful, FMap-grounded, unit-tested, LAW-0-verified on rendered frames** — NOT copy-pasted. Discipline: port live/observe paths only (Redux's best perception is reset-anchored; we ban mid-game reset); reject the monolith "route everything to one engine"; skip game-specific detector cruft.
- **Brick 11 — Logical-grid quantum lens** ← `grid_perception.detect_grid`+`_fidelity`+`cell_to_frame`. Detect the ~12×12 grid upscaled 5× to 64; KEEP raw (never downsample). Makes "move" = 1 logical cell = the 5-px step the pixels showed. (Supersedes the hand-rolled `motion_model.py`.)
- **Brick 12 — Agent-vs-environment separation** ← `transition_induction` ego/self-contact context split + `effect_classifier` discriminative-AND-axis-aligned cursor test. The real ls20 fix: learn the piece's move despite maze redraw churn; a static timer/HUD never confirms as caused.
- **Brick 13 — Regime classify + routing** ← `ControlRegime.classify` (avatar/actuator/unknown, re-evaluable, win-confirms); self_locus emits `n_controllable`; unknown routes to the prober. Reject the "everything→one engine" default.
- **Brick 14 — SpatialMap navigation** ← directed-edge traversability + decay + frontier/info-gain. Real maze navigation for a multi-pixel avatar (corner-clip aware).
- **Brick 15 — BudgetModel** ← induce meter/lives/price-per-action by watching frames; measure action cost (ls20's timer bar; price changes per level). "Measure, never guess the constant."
- **Brick 16 — Goal pursuit** ← `compile_discrepancy` (typed relation → scalar d=0) + `kernel` attribution split (OBJECTIVE_INVALID vs PURSUIT_FAILED) + abduction SHAPE (win-relation abduced from the goal's appearance, not the action space; detectors rebuilt as grammar molecules).
- **Brick 17 — Evolvability engine** ← mint/co-opt/escalate + BEFORE-state tautology guard + CUSUM speciation + level-split held-out. THE MAP's minting spec, already built.
- **Folded upgrades (apply when the brick is touched):** marketplace guards (veto-vs-select for no-change, recency-EWMA pricing, prestige≠budget, None-is-an-answer); falsified-ledger guards (earned-exhaustion taxonomy, paralysis→reopen, reproduce-before-mint + mask-own-instruments); reasoning-rides-every-action with a mandatory disproof field.

## Foundation status
The map's central bet — aim variation with a spec — has a **measured floor**: the reverse-map
falsifier on the real 46-framework F* kernel gave edge **1.29×**, p<0.001 vs a shuffled null.
E.1 does not fail. We build on ground that held its own test. (Fuller problem→primitive D.1
accrues for free as the FMap dogfooding loop runs on real build problems.)

## Log
- **2026-07-20** — branch created (orphan). Full control handed over.
- Brick 1 **falsified ledger** (§9.4) — 10 tests. FMap → adaptive_immune_system #1 (map's own choice); failure-mode audited.
- Brick 2 **full-res perception** (§9.2) — 9 tests. FMap → Simon near-decomposability (segmentation principle). Object permanence.
- Brick 3 **typed grammar** (§9.1) — 7 tests. FMap → small_world_network. "Typing beats size" measured (64×–1024× < flat bag).
- Brick 4 **the residual** (§8/§10/§11) — 9 tests. FMap dull (grounded in the map directly). Sense organ + un-suppressible PE-integral (brake invariant) + MDL mint test with the before-state tautology guard.
- Brick 5 **the marketplace** (§9.3/§7) — 7 tests. FMap → predictive_processing + goodharts_law GUARD. propose→vote→resolve; currency = the residual (informative accuracy); a trivial "nothing changes" hypothesis earns ZERO salience on a change frame (can't game the price); hallucinated change penalised.
- Brick 6 **two streams + pose + α** (§9.2/§3/I2a) — 7 tests. FMap → arrows_impossibility (no perfect aggregation → α must be LEARNED). alpha = learnable weight on private residual instinct vs social grammar; decaying LR (primacy → path-dependent). **The map's own I2a falsifier PASSES**: identical architecture + divergent encounter ORDER ⇒ divergent α (pose mechanism holds), with a same-order determinism control.
- Brick 7 **loop closure** (§9.6) — 9 tests. FMap → kauffman_nk_landscapes (tight coupling ⇒ untunable rugged landscape → the six modules stay LOOSELY coupled through clean interfaces, each swappable). One small COMPLETE loop wires all six bricks: PERCEIVE (segment+track the controllable) → THINK (generic transforms filtered by the falsified ledger) → MAP (price each by the residual it leaves) → ACT (commit the most-believed, pose-blended) → FEEDBACK (residual bus banks surprise, ledger records express-before-judge, pose.learn moves α, belief updates). **The loop LEARNS, measured**: on a toy world whose action→effect map is the WORLD'S SECRET (never passed to the agent), the agent discovers which action causes which transform purely from prediction error — argmax belief converges to the true mapping, committed residual falls, α moves, PE-integral grows monotone, refuted transforms aren't re-committed, no downsampling, two actions discriminated, and the secret is structurally never encoded (built with generic unit moves + empty belief).
- Brick 8 **hard-bound enforcers at import** (§VIII / the Constitution) — 11 tests. FMap → lambda_calculus (d=0.171): enforce by CONSTRUCTION, not by check — the forbidden op must be UNEXPRESSIBLE against the objects; its failure mode is variable capture (a mutable guard-flag silently rebound), guarded by exposing NO disable/bypass parameter anywhere (a free variable the caller could bind to escape) and putting the check on the MANDATORY path. `bounds.py`: `ResetGate` (RESET banned in active play; a credit is earned ONLY by a completed level — the exact Redux force-RESET bug, now unbypassable), `ResolutionBound` (once the canonical full-res shape is admitted, no smaller frame enters — no downsampling ever). Both take no arguments that relax them. The module runs a **constitutional self-check at import and raises ImportError if either bound fails** — you cannot even load a broken constitution. Wired into `AgentLoop` on the mandatory paths: `perceive()` admits every frame through the bound; `reset()` routes through the gate (raises `ResetForbidden` unless earned) and, when permitted, clears only the board — NEVER the learned lineage (belief/pose/ledger) and NEVER the brake's PE-integral (tested: an earned reset does not zero the record of surprise).
- Brick 9 **live-run wiring + first run through the gate** (§9.6 vs the real world) — 6 smoke tests (offline FakeSession, no network). `arc3_env.Arc3Session` (thin API session: open→step→close, native full-res frames, key env-only, holds NO game knowledge) + `live_run.run_live` (pure over the session interface: actions = the game's available non-click discrete actions as generic labels; earned-only reset via earn_progress + never reset in play; no-downsample on perceive; returns `levels_completed` + what it LEARNED). FMap not re-queried — this brick is integration of built parts, grounded in §9.6 directly.
  - **FIRST LIVE RUN (ls20, scorecard 002fc26f-52f7-41a4-9a68-620878a9c512):** `levels_completed = 0` over 262 real actions in 90s; outcome=action_cap; pe_integral 312; **only A1 ever exercised, A1→STAY belief 1.0**, α stayed 0.5, 4 refuted. RULE 0.7: the first run TEACHES, and it taught two concrete mechanism gaps → (a) **exploration collapse** — `choose()` greedily commits the single best (action,transform) across ALL actions, so once one converges the other actions are never tried (this is the documented ls20 explore-collapse pathology); (b) **STAY dominating** means unit-translation of the *smallest object* is the wrong controllable model for ls20 — either the smallest-object heuristic isn't grabbing the real controllable, or ls20's dynamics aren't single-object unit shifts. The loop correctly preferred the honest null (STAY) over a bad model, then couldn't escape it. NO answer encoded — this is a capability gap, not a solution.
- Brick 10 **exploration drive + self-locus controllable-ID** (the two gaps run-1 exposed) — 11 module tests + 1 loop regression. FMap → federated_learning (d=0.117: guarantee coverage, don't let one action dominate the aggregate) for exploration; goodharts_law (d=0.119: contingency not correlation) for self-locus. `exploration.Explorer` — count-based novelty bonus (untried action = full bonus, decays with visits → can't be farmed), added to `choose()`'s score so the loop cannot collapse onto one action. `self_locus.SelfLocus` — names the controllable as the colour whose motion is CONTINGENT on WHICH action (interventional contrast), guarding the Goodhart trap (an object that moves identically under every action, or without me, is autonomous, not self); wired into `perceive()` (falls back to smallest-object at cold start). Loop regression test: a 3-action world with 2 dead decoys now reaches coverage 1.0 (all tried), where run-1 collapsed to one.
  - **SECOND LIVE RUN (ls20, scorecard 678ece4c-1450-438e-bf23-697da9ff592c):** `levels_completed = 0`, 129 actions, outcome=**GAME_OVER**. **Exploration VALIDATED live: all four actions A1–A4 exercised** (run-1 exercised only A1). No transform earned belief.
  - **CORRECTION (from RENDERING the frames — the first telemetry read was WRONG):** the change is NOT "large/global." 95% of the grid (3875/4096 cells) is STATIC the whole run; the game lives in ~221 cells. **The pixels show a maroon block with a bright-green (lime) cap MOVING AROUND A MAZE** (green walls on yellow), plus a small bottom timer bar (~2 cells/step). ls20 is a MOVER/maze game (win at 7 levels), not global noise. The real reason belief stayed 0: **the residual is computed over the WHOLE frame, so the maze's per-step redraw churn (~50 cells, near-identical across all 4 actions → largely action-INDEPENDENT) drowns the moving piece's small action-caused signal.** Self-locus abstained for the same reason (colour-level whole-frame motion, not localized). This diagnosis is from `tools/render_run.py` images, not numbers.
- **PIVOT (harvest Redux — see claude/HARVEST_from_redux.md):** bricks 1–10 underpowered; port Redux's proven mechanisms through the lens. New sequence bricks 11–17 (above).
- Brick 11 **logical-grid quantum lens** (harvested from Redux `grid_perception`) — 7 tests. FMap → arrows_impossibility (d=0.115: no vote picks the true grid; the arbiter is a non-gameable FIDELITY GATE) + dijkstra_separation_of_concerns (lens is a coordinate service, not a replacement rep). `logical_grid.py`: `detect_grid` (finest reliable pitch → centred origin → commit only if near-lossless) + `GridSpec` (stride, `cell_to_px`/`px_to_cell` for dead-centre clicks, `cell_colour`/`logical_read` = NON-DESTRUCTIVE reads). **Map bound honoured: NEVER downsamples the working frame** (Redux's `downsample()` deliberately omitted; test asserts it's absent).
  - **LAW-0 FINDING (tested detect_grid on the real ls20 frame BEFORE building):** it does NOT recover ls20's game grid. ls20's true 5-px grid scores fidelity 0.818; the only tiling passing a 0.93 gate is a **spurious 2-px/24×24** one (0.946) — not the game grid. So the gate is set NEAR-LOSSLESS (default **0.98**, principled: a glyph is exact, losing ~5% destroys real structure): ls20 is correctly **rejected as native**. **ls20's 5-px stride therefore comes from MOTION (brick 12: effect_classifier `cell_scale` = median observed displacement), NOT from tiling.** The lens is real infra for genuinely-tiled CLICK boards, a different game family. (This corrected the heartbeat's premise that the lens would give ls20's stride — the pixels said otherwise.)
- Brick 12 **agent-vs-environment separation** (harvest `effect_classifier.robust_action_map` + `cell_scale`) — 6 tests. FMap → goodharts_law (d=0.112: consistency alone is gameable; require the motion to VARY with the action). `agency.CursorAgency`: the cursor is the colour whose per-action centroid displacement is CONSISTENT (majority shift) AND DISCRIMINATIVE (differs across actions) AND AXIS-ALIGNED (≥2 distinct) — background drift moves the same regardless of action → rejected; a static HUD never moves → rejected. Recovers the STRIDE for free (median axis-aligned shift). Wired into the loop: `perceive()` prefers the agency cursor, `choose()` predicts the true multi-cell MOVE, `observe()` feeds agency. Integration test: a mover world with heavy action-independent churn where whole-frame unit-belief stays <0.2 (drowned, reproducing ls20) but agency learns the true 3-cell map.
  - **LAW-0 catch (replayed the real recording through the agency, network-free):** first pass gave cursor=colour9 stride=**2** — WRONG. The pixels explain it: colour 9 (maroon) is in the moving block AND static glyphs (top key-icon, legend), so the mean-of-all-cells centroid was diluted. Fixed `_centroid` to track the LARGEST CONNECTED COMPONENT (Redux's approach). Re-verified on real ls20: **cursor=colour12 (lime cap), stride=5, map A1↑5/A2↓5/A3←5/A4→5** — exactly the hand-measured truth. Rendered frames confirm the maroon+lime block is what responds to actions (the central cross is fixed).
  - **THIRD LIVE RUN (ls20, scorecard 50d29d18):** levels 0, GAME_OVER — EXPECTED: brick 12 fixes perception/separation only; the agent now knows its body + move map but has no GOAL or NAVIGATION yet (it wanders the block until the timer runs out). Those are the level-unlockers.
- Brick 13 **grid navigation + goal abduction** (harvest `spatial_map` + `goal_cues`) — 8 tests. FMap → adaptive_immune_system (d=0.144: remember SPECIFIC measured walls, defeasibly). `navigation.GridNav`: directed-edge walls learned from whether a committed MOVE actually moved the cursor (a blocked move = wall; unmeasured = optimistically passable — never invent a wall unhit), BFS toward the goal with LEAST-VISITED expansion (breaks the 2-cycle a naive optimistic replanner falls into at a wall) + a least-visited fallback, decay + invalidate. `goal.abduce_target`: a unique small non-self object is a provisional destination (Redux `detect_distinct_exit` shape). Wired: once the full move-map is learned (coverage-first), `choose()` navigates the cursor toward the abduced target through non-blocked edges; `observe()` records wall outcomes. Loop test: navigates a cursor to a target around a wall (reroute one row off the direct line), learning the wall by bumping.
  - **HONEST SCOPE (LAW 0, verified by replaying the real ls20 frame — no network run needed):** (1) navigation solves reroute-around-a-reasonable-obstacle, but NOT an adversarial hidden narrow gap far from the direct line (that needs systematic frontier exploration — a later refinement / fuller `spatial_map` harvest). (2) `abduce_target` returns **None on ls20**: it has no unique small landmark (the key-glyph colour repeats across the block/legend/glyph; grey boxes repeat), so the "distinct exit" cue doesn't fire and navigation doesn't engage yet. This precisely defines brick 14.
- Brick 14 **goal by reward-disposes** (harvest `objective_abductor` cue-proposes/reward-disposes) — 7 tests. FMap → hayek_price_system (d=0.154: reward is the PRICE selecting among candidate goals) + adaptive_immune_system (d=0.137: demote defeasibly, never hard-drop). `goal.GoalManager`: a market of candidate goal cells priced by reward — `propose` seeds candidates (each distinct non-self object, marker-like first), `active_goal` pursues the highest-priced, `observe` CONFIRMS on a reward while pursuing (price up) or DEMOTES on reaching-without-reward / stall (price down, rotate; residual price kept — reconsiderable). Wired: `choose()` navigates toward the active goal; `signal_reward()` (called by live_run on level-up) confirms it; `observe()` disposes. Loop test: a decoy + a real target — the agent demotes the decoy and prices the rewarding target above it.
  - **FOURTH LIVE RUN (ls20, scorecard 68752dc6):** levels 0, GAME_OVER, but a QUALITATIVE LEAP — **the agent now genuinely navigates the maze** (89 distinct cursor cells vs shuffling before) and, per the RENDERED frames, **moved the block UP the corridor toward the key-glyph in the grey box at the top** (the game's apparent goal). It found and approached the objective; it just didn't trigger a win within the timer. Agency still perfect (cursor 12, stride 5, map A1↑5/A2↓5/A3←5/A4→5).
  - **Why not a level yet (from the pixels):** reaching an abduced candidate didn't grant a level — the win is likely finer than BE_AT-any-distinct-object (enter the grey box / the exact key cell / a TOUCH/PUSH/collect relation), and the goal market rotates through candidates spending the timer. Needs: (a) BudgetModel (runs die at ~130 steps on the timer — commit decisively, don't wander), (b) finer goal targeting toward the key-glyph, (c) maybe a richer win-relation than BE_AT.
- Brick 15 **BudgetModel + decisiveness** (harvest `budget_model`) — 5 tests. FMap → optimal_foraging_theory (d=0.180: commit to the best patch as budget depletes, but abandon a patch whose return dropped). `budget.BudgetModel`: induces the remaining-action budget by watching the bottom HUD bar — the meter is the band colour whose span only DECREASES; actions_left = bar LENGTH / MEASURED cols-per-action (Redux's lesson: measure the length, never divide area by a guessed rate). Wired: `observe()` watches the bar; as `fraction_left` drops below 0.4, goal stall tolerance triples (commit harder to the best goal). Tests: meter induced + drain rate measured from a shrinking-bar toy; pursuit gets more persistent as budget drops.
  - **FIFTH LIVE RUN (ls20, scorecard 61363d34):** levels 0, GAME_OVER (131). Budget induces fine (30 walls, 9 candidates), but decisiveness didn't change the outcome — the top goals all demoted to ~0.2 (tried, none rewarded). **LAW-0 investigation (rendered + measured): the block tops out at row 15.5 — the grey key-box (row ~12) is WALLED; the key-glyph (row 11-13) is UNREACHABLE by moving up, reaching the box entrance does NOT win, and the box does not react to the block.** So ls20's win is genuinely NOT "navigate the block to a distinct object" — the whole navigate-to-object approach can't crack it, and budget only affects HOW LONG it wanders.
- Brick 16 **multi-game experiment** (break the ls20 tunnel — RULE 0.7) — no code, an information-gathering run. Ran the general stack on 5 games (see claude/NEWHORSE_multigame.md). **RESULT: 0 levels anywhere, but the MOTOR/PERCEPTION layer GENERALIZES** — on ls20/ka59/sp80 the agent identified its body + exact move map from scratch (colour 12/14/9, stride 5/3/4) and navigated a mover toward targets; ka59 confirmed by RENDER (marker driven across a corridor to a yellow-square target — reaching it doesn't win). tr87 = no discriminative mover; ft09 = click-only (out of scope). **The universal blocker is the OBJECTIVE, not the motor system:** "BE_AT a distinct object" is too narrow — real ARC-3 wins are specific typed relations (fit-into/cover/match/collect/contact-trigger/…) that must be DISCOVERED. The 12-brick motor stack works; the cognitive/objective layer is the gap.
- Brick 17 **typed win-relation space** (harvest `objective_grammar`) — 10 tests. FMap → hayek_price_system (d=0.108: reward is the price that reveals which relation wins). `relations.candidate_relations`: proposes a SPACE of typed relation-hypotheses per object — **BE_AT** (on the object's frame, snapped to an actual cell), **TOUCH** (adjacent), **COVER** (inside a hollow slot — frame-detected). `GoalManager` refactored relation-agnostic (`observe(reached, reward)`) and given `credit(win_cell)` — a reward is attributed to the relation whose target is the cell the cursor actually WON at (its pre-respawn destination), not the nominally-active one (fixes credit mis-attribution). Integration test (the map's point): a world whose win is COVER (enter a hollow slot) with a decoy BE_AT — **the agent DISCOVERS COVER is the winning relation by reward**, not by assumption.
  - **LIVE ls20 + ka59:** still levels 0. The market proposes/prices typed relations correctly (ls20 BE_AT variants; ka59 BE_AT/TOUCH, and COVER IS proposed for its hollow yellow squares — verified by replay), but none rewarded. **LAW-0 finding on ka59:** there are TWO magenta markers (colour 14) and TWO yellow slots (colour 4) — the win is almost certainly a QUANTIFIED/COMPOSITE relation (ALL markers COVER ALL slots), beyond a single-target relation. ls20 similarly resists single-target relations.
- Brick 18 **quantified objectives** (harvest `objective_grammar` ALL/SOME/NONE) — 2 tests. FMap → ostrom_iad_framework (d=0.104: a rule over the whole SET) + lindblom (reduce violators one at a time) + goodharts_law GUARD (one member ≠ the whole set). `relations.quantified_candidates`: `ALL(x in COLOUR-CLASS)` for any colour appearing ≥2× (visit-all / collect / all-markers). The loop pursues the NEAREST UNVISITED member; the objective is "reached" only when ALL members are visited (goodhart guard). `GoalManager.credit` extended to confirm the active quantified ALL on a reward. Integration test: a world whose win is "visit BOTH markers" — the agent DISCOVERS `("ALL", colour)` is the winner; visiting one does not win.
  - **LIVE ka59 + ls20:** still levels 0. Quantified hypotheses ARE proposed + priced (ka59 `('ALL',4)`, ls20 `('ALL',5)`) but none confirmed in the ~100–132-step budget. **Honest read:** ka59's win is likely a 2-marker ASSIGNMENT (the two magenta markers into the two slots) — one cursor visiting slots can't satisfy that (it needs push/multi-controllable mechanics); ls20 similarly resists. The quantified layer is proven on a toy but doesn't crack these specific hard games in budget.
- Brick 19 **broad game sweep — ★ FIRST ARC-3 LEVEL CLEARED ★** (experiment; see claude/NEWHORSE_sweep.md). Ran the stack on 10 untried games. **m0r0: levels_completed = 1 — the agent WON a level by its own reasoning** (verified: 0→1 at frame 68; winning goal `('ALL', 5)` confirmed at price 11.0). The architecture is validated END-TO-END: body-ID (cursor 10, stride 5) → navigate → propose a QUANTIFIED objective → satisfy it → WIN → reward confirms it. No answer encoded. Sweep: ~9 games engage as movers; wins where the objective is a discoverable quantified visit/arrange within budget; loses where it needs push/multi-controllable (ka59), clicks (ft09/s5i5), a novel relation (ls20), or has no discriminative mover (cd82/lf52/su15/tr87).
- Brick 20 **capitalise on the win** (experiment; see claude/NEWHORSE_sweep.md). **m0r0 clears level 1 RELIABLY (2/2 runs)** — a reproducible ARC-3 solve. Level 2 is a DIFFERENT, harder puzzle (magenta maze) → GAME_OVER. More budget did NOT help tu93/dc22/bp35 (early GAME_OVER regardless). **LAW-0 finding (rendered tu93): it's a maze-to-exit game where the yellow cursor stays STUCK in the corner — navigation can't solve the maze's narrow corridors, so the timer runs out.** That's the deferred brick-13 limitation, and it's the HIGHEST-LEVERAGE next capability (tu93 is a clean maze the agent should win with proper pathfinding). Blocker taxonomy recorded: hard-maze nav (tu93/dc22/bp35/m0r0-L2), push/multi-controllable (ka59), click modality (ft09/s5i5), novel-relation minting (ls20), no-mover perception (cd82/lf52/su15/tr87).
- **Suite: 125/125.** Next: **brick 21 → HARD-MAZE NAVIGATION UPGRADE** (harvest the rest of `spatial_map`: frontier_step / reachable / info-gain). When the goal isn't reachable through known-free cells, systematically sweep the FRONTIER toward the goal so narrow-corridor mazes solve. Unit-test on the adversarial-maze case brick 13 currently fails (the hidden-narrow-gap world). Then re-run tu93 (RENDER + verify the cursor reaches the exit) — a clean shot at a 2nd game win. Deferred: push/multi-controllable; click modality; minting.
