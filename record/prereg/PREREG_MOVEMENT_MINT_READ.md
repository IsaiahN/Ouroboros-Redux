# PREREG — THE MOVEMENT-MINT READ (2026-08-21, runs after the clean deploy)

**The question:** why is Γ click-only (2,094 atoms, all action 6) when the wiring is
action-agnostic end to end? **The hypothesis:** egocentric movement scrolls the whole view,
so the half-board bbox clause rejects every movement transition — a metal key measuring
change extent in a frame of reference where moving the observer changes everything.

## The two branches, named before the read (neither may be rescued into the other)
1. **REPRESENTATIONAL (the metal key):** movement transitions overwhelmingly exceed the
   half-board clause. Fix shape: ego-shift compensation before the bbox test — the guard
   measures change relative to the agent, not the frame. Movement becomes mintable; the
   composer gets a movement vocabulary; the library problem was over-specification.
2. **ELSEWHERE (the branch to hold seriously, being the less expected):** movement
   transitions largely PASS the clause and still don't mint. The click-only census becomes
   the anomaly to explain, the killer is another guard (support? the premium? the offer
   path's residual bar?), and the composer's sizing question stays open. **The verdict
   reason field names the actual killer directly in this branch.**

## Two instruments, either sufficient, both running
- **The reason count (primary, cheapest):** post-deploy, every mint reject carries its
  `reason`. Count reject reasons on movement-heavy boxes (bp35, tu93, wa30, m0r0).
  **N = 100 movement-attributable verdict records** across those boxes — at their write
  rates, minutes of play. `bbox_half_board` dominating → branch 1. A spread or a different
  dominant reason → branch 2, with the killer already named.
- **The frame read (confirmatory):** from clean post-fix trace rows, bbox/board fraction
  per movement transition. **N = 30 clean movement transitions per game, 3 games (90
  total)** — a true rate ≥0.9 vs ≤0.5 separates decisively at that n; no statistic
  finer than a proportion is needed. Also minutes at observed rates.

## The rebuilt action book's expectation, pinned (Seat 4)
The clean-window rebuild starts from an empty post-fix window: **expect the inverse-pair
count to drop sharply from 179 — that is the taint leaving, not evidence vanishing.**
Fewer trustworthy pairs beat many untrustworthy ones; the drop's SIZE is itself a
measurement of what the lossy writer cost. Report old-n → clean-n per game without
averaging the two populations.

## Sequencing
Deploy (verified commit, HOLD lift) → minutes of live play → reason count → frame read →
branch named → **only then does the composer prereg get sized.**
