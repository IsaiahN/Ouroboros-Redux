# THE ASSEMBLY (Isaiah's directive 2026-08-12): build the whole policy-maker loop, then restart testing

**Live game testing PAUSED (census + compound2 stopped; fabrics/banks preserved). Unit gates
run every wave — they are the only safety while building fast. Live falsifiers (census,
debasement, capability suites) all run at RESTART against the assembled machine.**

## WAVE 1 — foundations (parallel, disjoint files)
- **W1a `effects.py`** — the EFFECT constructor + typed Γ: single-atom temporal transform
  (canonical context patch + action → transform), regime-tagged, fabric-backed; composites
  keyable as atoms; type field lexical|structural; the n=1 synthetic gate (EFFECT beats route
  on one example) as its acceptance test. Reference: Nexus operator_effect.py.
- **W1b `binder.py`** — the role binder: one invariance classifier, four verdicts
  (BODY=contingent mover [subsumes self-locus] / WORKSPACE=mutates under contact /
  REFERENCE=invariant under agent / RESOURCE=monotone under action); publishes BINDINGS
  (object-class → slot + confidence), re-binds on level change; unbound = None.

## WAVE 2 — banking + routing
- **W2a predictor bank** — one predictor per bound slot, EVERY slot bets every action through
  BetBook (multi-slot); a slot that can't bet isn't read.
- **W2b residual router** — per-slot three-binning (TRANSFERRED / NOVEL / BROKEN·rebinding /
  BROKEN·mechanism); NOVEL → the import queue; BROKEN·mechanism → the mint queue;
  BROKEN·rebinding → binder re-fit.

## WAVE 3 — the mind
- **W3a the MDL mint** — SUPPORT × REACHABILITY × NOVELTY as a product; accept iff
  |φ| + |R|φ| < |R|; mints EFFECT atoms from routed residuals; quarantine/re-derivation on
  zero factors.
- **W3b discrepancy engine `d`** — role-aware transform-MATCH vs the REFERENCE slot → axis-wise
  residual objective; `d` is a HYPOTHESIS: d→0 without level advance ⇒ objective falsified to
  pariah (the anti-elaboration check).
- **W3c planner** — search over available EFFECT atoms for a multiset+order driving d→identity;
  feasibility vs measured per-level cost; stopping test d=0 (no periods learned); drive gated
  by the WHEEL RULE (salience-grounded: repeatedly-verified EFFECT atoms = informed play).

## WAVE 4 — regulation + integration
- **W4a affect** — four gains off the settlement ledger (seed/width/depth/mint-bar), bounded,
  hysteretic; the REPLAY TEST as a pytest (affect(t)=f(ledger[0:t]), byte-exact offline).
- **W4b verdict circuit completion** — mute → quarantine + empowerment probe.
- **W4c integration pass** — wiring audit (every producer has its consumer named), full gate,
  containment re-check, THEN THE RESTART: fresh compounding swarm with everything live; the
  paused falsifier suites run against the assembled machine.
