# WIRING REGISTRY (rung 0c, THE_LADDER.md) — one row per organ

THE DONE STANDARD: a build is DONE when something in the live path calls it
with real inputs. This file is the declaration layer; it states reality, it
does not hide it. It is CHECKED, not trusted:

* deterministic gate: `tests/gate/test_wiring_registry.py` — every LIVE row's
  receipt region (±30 lines) still references the symbol AND the symbol is
  referenced from a production module (non-import AST scan; `__init__`
  re-exports do NOT count — the vulture blind spot). Every SEVERED row asserts
  the INVERSE, so a silent reconnection goes red until this file is updated.
  Every class in `engines/egocentric/*.py` must have a row; a new organ with
  no receipt is UNSHIPPABLE.
* empirical gate: `tools/live_coverage_diff.py` — branch coverage from a
  bounded synthetic episode diffed against the LIVE rows; a receipt whose line
  never executes is reported SEVERED-EMPIRICAL (`severed count=N` is the
  rung-0c line). A real-game operator-invoked run is the stronger mode; the
  synthetic run is the CI-safe floor.

Row format (machine-parseable; the gate parses exactly this):
`| name | symbol (module:qualname) | site (file:line) | status | date | note |`
Status ∈ LIVE / SEVERED / DELETED-PENDING. For LIVE rows the site is the
claimed live-path call site; for SEVERED rows it is the WIRE-BREAK line.
Note markers the gate honors: `[helper]` (own-module reference counts),
`[alias=NAME]` (consumed under another name), `[referenced-but-dead]`
(deterministically referenced, semantically dead — inverse check waived
loudly; the coverage diff owns it).

Seeded 2026-08-16 from PIPELINE_AUDIT.md (the full sweep: eleven severed
organs behind green tests + verified-LIVE lists 2/3), receipts re-derived by
build-scan the same day.

## LIVE WIRES

| name | symbol | site | status | date | note |
|------|--------|------|--------|------|------|
| fabric-core | engines.egocentric.fabric:KnowledgeFabric | cognitive_loop.py:1812 | LIVE | 2026-08-16 | lazy init + seed once per game; also cognitive_game_player.py:107 (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1492) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1558) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1618) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1735) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 1812, drift +37; was cognitive_loop.py:1775) |
| observer-core | engines.egocentric.observer:EgoObserver | cognitive_loop.py:1781 | LIVE | 2026-08-16 | replay-path twin at cognitive_loop.py:1388 (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1464) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1530) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1587) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1704) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 1781, drift +37; was cognitive_loop.py:1744) |
| tracker-core | engines.egocentric.perception:ObjectTracker | engines/egocentric/observer.py:27 | LIVE | 2026-08-16 | also engines/perception/event_detector.py:104 |
| perception-object | engines.egocentric.perception:Object | engines/egocentric/goal.py:131 | LIVE | 2026-08-16 | the typed percept unit; used across goal/observer |
| downsample-ban | engines.egocentric.perception:DownsampleError | engines/egocentric/perception.py:125 | LIVE | 2026-08-16 | [helper] raised in own module; perception is live via tracker-core |
| self-locus | engines.egocentric.self_locus:SelfLocus | engines/egocentric/observer.py:28 | LIVE | 2026-08-16 | contingency self-detection inside the observer |
| goal-manager | engines.egocentric.goal:GoalManager | engines/egocentric/spine.py:32 | LIVE | 2026-08-16 | verbatim port wrapped by GoalSpine |
| goal-spine | engines.egocentric.spine:GoalSpine | cognitive_loop.py:1784 | LIVE | 2026-08-16 | replay twin cognitive_loop.py:1391; drive() pre-empt at cognitive_loop.py:1066 (the ONE wheel site) (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1479) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1545) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1590) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1707) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 1784, drift +37; was cognitive_loop.py:1747) |
| click-economy | engines.egocentric.spine:GoalSpine.credit_click | cognitive_loop.py:2063 | LIVE | 2026-08-16 | drive_click cognitive_loop.py:1074; falsify write-back cognitive_loop.py:1922; credit the ACTED-ON cell (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1618) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1684) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1863) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1986) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2063, drift +37; was cognitive_loop.py:2026) |
| bet-spine | engines.egocentric.betting:BetBook | cognitive_loop.py:1672 | LIVE | 2026-08-16 | commit cognitive_loop.py:1311; settle cognitive_loop.py:2110; committed≠executed → VOID (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:1290) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1338) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1479) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1596) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 1672, drift +37; was cognitive_loop.py:1635) |
| pricing-salience | engines.egocentric.pricing:informative_salience | engines/egocentric/betting.py:98 | LIVE | 2026-08-16 | the live half of pricing.py; Marketplace stays iced (see marketplace-iced) |
| efference-copy | engines.egocentric.binder:RoleBinder.predicted_change_mask | cognitive_loop.py:2129 | LIVE | 2026-08-16 | CK-2a; subtraction consumed at cognitive_loop.py:1709 (observe_attributed) (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1684) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1750) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1929) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2052) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2129, drift +37; was cognitive_loop.py:2092) |
| binder-core | engines.egocentric.binder:RoleBinder | cognitive_loop.py:2089 | LIVE | 2026-08-16 | W4c lazy-init block; fed per step at cognitive_loop.py:1709 (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1652) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1718) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1889) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2012) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2089, drift +37; was cognitive_loop.py:2052) |
| binder-evidence | engines.egocentric.binder:_ClassEvidence | engines/egocentric/binder.py:80 | LIVE | 2026-08-16 | [helper] internal accumulator of the live binder |
| gamma-store | engines.egocentric.effects:Gamma | cognitive_loop.py:2095 | LIVE | 2026-08-16 | typed EFFECT store over the fabric (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1651) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1717) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1896) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2019) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2095, drift +36; was cognitive_loop.py:2059) |
| bank-core | engines.egocentric.bank:PredictorBank | cognitive_loop.py:2090 | LIVE | 2026-08-16 | settle at cognitive_loop.py:1733; every settlement routes (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1653) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1719) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1890) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2013) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2090, drift +37; was cognitive_loop.py:2053) |
| router-core | engines.egocentric.router:ResidualRouter | cognitive_loop.py:2106 | LIVE | 2026-08-16 | constructed MINERLESS — the miner wire is the conditional-miner SEVERED row (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1657) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1723) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1891) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2014) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4.5 structural repair, LOUDLY: this receipt had ALREADY rotted when the repair began -- the COMPOSER STAGE 4 SETTLEMENT build's insertions above this site moved it past the +/-30 drift tolerance (measured at 73ae715: site 2102 vs receipt 2065, drift +37) and its refresh sweep MISSED these two rows -- router-core and mint-core are the only cognitive_loop.py rows without that build's refresh note -- so they were committed red; the stage-4.5 insertions above (+8) took the tree to 2110, drift +45; the repair then moved the stage-4.5 self-cell stamp in record_result from INSIDE the .credit window (4 lines above this site) to below the .route anchor (7 lines below it), net -4 here; the true site is the CONSTRUCTOR CALL `self._residual_router = ResidualRouter()` in record_result's W4c lazy-init block -- a call site, not a docstring mention; measured `grep -n` -> 2106; was cognitive_loop.py:2065) |
| mint-core | engines.egocentric.mint:MDLMint | cognitive_loop.py:2107 | LIVE | 2026-08-16 | consider() at the credit/primal sites; ledger every verdict (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1658) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1724) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1892) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2015) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4.5 structural repair, LOUDLY: this receipt had ALREADY rotted when the repair began -- the COMPOSER STAGE 4 SETTLEMENT build's insertions above this site moved it past the +/-30 drift tolerance (measured at 73ae715: site 2103 vs receipt 2066, drift +37) and its refresh sweep MISSED these two rows -- router-core and mint-core are the only cognitive_loop.py rows without that build's refresh note -- so they were committed red; the stage-4.5 insertions above (+8) took the tree to 2111, drift +45; the repair then moved the stage-4.5 self-cell stamp in record_result from INSIDE the .credit window (4 lines above this site) to below the .route anchor (7 lines below it), net -4 here; the true site is the CONSTRUCTOR CALL `self._mdl_mint = MDLMint(self._gamma)` in record_result's W4c lazy-init block -- a call site, not a docstring mention; measured `grep -n` -> 2107; was cognitive_loop.py:2066) |
| affect-seed-steer | engines.egocentric.affect:AffectGains.seed_gain | cognitive_loop.py:2423 | LIVE | 2026-08-16 | B5 applied-bias site; AffectGains constructed cognitive_loop.py:1659; starvation+swallow widened, bounded (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1889) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1955) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2176) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2299) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2423, drift +54; was cognitive_loop.py:2369) |
| affect-starvation-steer | engines.egocentric.affect:AffectGains.starvation_steer | cognitive_loop.py:2498 | LIVE | 2026-08-16 | R1 explore_boost inside the mute-probe block; synthetic coverage floor may not reach it (RECEIPT REFRESHED 2026-08-19 by the REFIT-DESTINATION build, LOUDLY: rotted BY THIS BUILD -- the BROKEN-rebinding drain, ~35 lines, was inserted above it, exceeding the +/-30 drift tolerance by 5; measured `grep -n starvation_steer cognitive_loop.py` -> 2060, drift +35; was cognitive_loop.py:2025. SECOND TIME THIS ROW HAS ROTTED BY INSERTION-ABOVE in eight days -- see the 2026-08-18 refresh below -- which makes it a RECURRING receipt rather than a one-off, and the recurrence is the signal: this row sits directly beneath the cognitive loop's most-edited region. PRIOR: RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THAT BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it; was cognitive_loop.py:1959) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2060) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2251) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2374) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2498, drift +54; was cognitive_loop.py:2444) |
| lp-steer | engines.egocentric.lp_drive:LPDrive | engines/egocentric/affect.py:173 | LIVE | 2026-08-16 | consumed via AffectGains.lp_steer at cognitive_loop.py:3730; bounded, narrated, lp arm only |
| starvation-settle | engines.egocentric.starvation:StarvationBook | cognitive_loop.py:896 | LIVE | 2026-08-16 | settle_episode at end_game; ≤1 per socket (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:577) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:620) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:755) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:856) |
| swallow-settle | engines.egocentric.swallow:SwallowBook | cognitive_loop.py:935 | LIVE | 2026-08-16 | settle_episode at end_game; ≤1 per block (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:614) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:657) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:794) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:895) |
| consumer-drain | engines.egocentric.consumer:consume | cognitive_loop.py:916 | LIVE | 2026-08-16 | W1 once-per-episode budgeted drain (budget_n=8); 2026-08-17: PHASE 2 now takes its agenda from the RANKED drain (drain-ranked row) instead of pending() FIFO (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:594) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:637) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:775) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:876) |
| drain-ranked | engines.egocentric.consumer:_drain_order | engines/egocentric/consumer.py:742 | LIVE | 2026-08-17 | [helper] PREREG_DRAIN_ORIGIN.md §A — the ONE drain call site inside consume() (consumer.py:742), reached live via consumer-drain (cognitive_loop.py:594). Ranks the NEWEST DRAIN_WINDOW=512 pending records (KNOBS G21, Register G, GUESSED) by CHARACTERIZED-FIRST → LARGEST RESIDUAL → NEWEST; bounded by construction, never a whole-queue sort over the ~370k pre-characterization backlog. ABLATION (CLAIM.md's binding clause): env DRAIN_RANKED=0 / module flag consumer.DRAIN_RANKED=False returns pending() VERBATIM — oldest-first, byte-identical streams on disk, shipped as a PASSING test, not an intention. Gate tests/gate/test_ranked_drain.py |
| origin-marker | engines.egocentric.effects:origin_of | engines/egocentric/consumer.py:968 | LIVE | 2026-08-17 | (SITE REFRESHED 945 -> 968 by COMPOSER STAGE 4.5: consumer.extent_bargain was added above seed_imports; the marker and its read site are unchanged.) PREREG_DRAIN_ORIGIN.md §B — the POSITIVE provenance marker. WRITE SITES: effects.Gamma.add (effects.py:982) stamps origin + mint_seq on every atom record at write time (NOTE-REFERENCE REFRESHED 2026-08-20 by the W2 STAGE 2 CONTEXT-MIN build (PREREG_W2_STAGE2_CONTEXT_MIN.md), LOUDLY: rotted BY THIS BUILD -- the DONT_CARE sentinel block (~+17 at module top) and the context-minimisation section _ring_of/context_retained_cells/minimise_atom (~+88 above Gamma) were inserted in effects.py, moving Gamma.add further than the +/-30 drift tolerance; was effects.py:859. PRIOR: NOTE-REFERENCE REFRESHED 2026-08-20 by the W2a-2 VECTORISED ANCHOR SCAN build (PREREG_W2A2_VECTORISE_ANCHOR_SCAN.md), LOUDLY: rotted BY THAT BUILD -- the vectorised scan block _context_anchors/_shape_anchors + dispatch (~104 lines net +88) was inserted above Gamma in effects.py; was effects.py:772; the gate-checked READ SITE consumer.py:823 did not move); the local mint path passes origin="local" explicitly at mint.py:457 (NOTE-REFERENCE REFRESHED 2026-08-20 by THE VERDICT REASON FIELD build (W4: every mint verdict names its deciding clause; gate tests/gate/test_verdict_reason.py), LOUDLY: rotted BY THIS BUILD -- the module-docstring reason paragraph (~+16), the REASON_* vocabulary constants (~+26), the _record reason/failed/changed/mdl parameters + the _mdl_failed helper (~+38) and the per-terminal reason stamping in consider() (~+14) were inserted above the local mint path, moving it beyond the +/-30 drift tolerance; was mint.py:365; the gate-checked READ SITE consumer.py:907 did not move) (PRIOR: NOTE-REFERENCE REFRESHED 2026-08-20 by the W2 STAGE 2 CONTEXT-MIN build, LOUDLY: the EXTENT_RATE constant block + docstring additions + the premium term were inserted above it; was mint.py:280) (NOTE-REFERENCE REFRESHED AGAIN 2026-08-20 by the RE-POINT AMENDMENT build (PREREG_W2_STAGE2_CONTEXT_MIN.md, THE RE-POINT AMENDMENT), LOUDLY: rotted BY THIS BUILD -- the module-docstring re-point paragraph, CONFLICT_SCAN_CAP, the __init__ signature-index state and the _signature_merge call in consider() were inserted above the local mint path, moving it beyond the +/-30 drift tolerance; was mint.py:329; effects.py's Gamma.add write site also shifted to effects.py:994 by the ctx_conflict_cells pin block in minimise_atom (was effects.py:982, +12, WITHIN tolerance -- noted for accuracy, not rot); the gate-checked READ SITE consumer.py:823 did not move); consumer.seed_imports passes origin="imported" + source_game + source_seq at consumer.py:943. READ SITE (this receipt): seed_imports' already-have scan, consumer.py:907 — the live-path caller. Three words, not two: a pre-marker record reads ORIGIN_UNKNOWN and NO consumer may infer local from absence (mechanically asserted). Instrumentation only, additive, old books unchanged. Gate tests/gate/test_origin_marker.py (RECEIPT REFRESHED 2026-08-20 by the IMPORT ADMISSION GATE build (the extent premium at the DOOR; import-admission-gate row below, gate tests/gate/test_import_gate.py), LOUDLY: rotted BY THIS BUILD -- the module-docstring gate paragraph (~+20 at consumer.py top), the KIND_IMPORT_REJECT constant, admission_price (~+47 immediately above seed_imports) and the refused-scan + refusal path inserted INSIDE seed_imports moved the gate-checked READ SITE (the already-have scan) from consumer.py:823 to consumer.py:907 (+84, beyond the +/-30 drift tolerance; was consumer.py:823) and the imported WRITE SITE from consumer.py:836 to consumer.py:943 (was consumer.py:836); within-tolerance drift noted for accuracy, not rot: decline-branch's match def consumer.py:285->310 and drain-ranked's consume-loop call consumer.py:742->767, both +25, both rows left standing) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 1 build (PROPOSAL_COMPOSER_DESIGN.md §6.1, THE FACTORING; gate tests/gate/test_composer_stage1.py), LOUDLY: rotted BY THIS BUILD -- the COMPOSITE pricing branch inserted at the top of admission_price (~+38) plus the top-level applicability import (+1) moved the gate-checked READ SITE (seed_imports' already-have scan) from consumer.py:907 to consumer.py:945, beyond the +/-30 drift tolerance, and the imported WRITE SITE from consumer.py:943 to consumer.py:981; within-tolerance drift noted for accuracy, not rot: the local mint path's origin="local" pass moved mint.py:457 -> 467 (+10, the psig stamp block inserted above it in consider()) and effects.Gamma.add moved effects.py:994 -> 995 (+1, the psig cache pop in minimise_atom); the marker's semantics, fields and readers are UNTOUCHED by this build) |
| consumer-seed | engines.egocentric.consumer:seed_imports | cognitive_loop.py:93 | LIVE | 2026-08-16 | W1 seed side; called from _seed_imp at cognitive_loop.py:1668  (RECEIPT REFRESHED 2026-08-20 by the W1 narration build, LOUDLY: bet-before-act insertions moved sites; was :93) |
| import-admission-gate | engines.egocentric.consumer:admission_price | engines/egocentric/consumer.py:984 | LIVE | 2026-08-20 | [helper] (SITE REFRESHED 961 -> 984 by COMPOSER STAGE 4.5: admission_price no longer states the inequality itself -- both its branches delegate to consumer.extent_bargain, THE ONE STATEMENT the composer's local mint also pays (admission-bargain row); verdicts byte-identical, the atom branch passing its own encoding cost.) THE EXTENT PREMIUM AT THE DOOR (Seat 3 approved; PI_REPLAY_RESULT.md x PREREG_W2_STAGE2_CONTEXT_MIN.md): the mint priced every LOCAL atom's context extent (mint.EXTENT_RATE) while network arrivals entered Gamma unpriced -- a door with a wall beside it. THE ADMISSION POINT: seed_imports prices every candidate at consumer.py:923 (this receipt, the ONE call site) BEFORE the Gamma write at consumer.py:942, with the mint's OWN inequality -- cost = effects.encoding_cost_atom + mint.EXTENT_RATE x effects.context_retained_cells vs R = mint.RESIDUAL_CELL_COST x changed + mint.UNEXPLAINED_PREMIUM under mint.MDL_MARGIN; constants IMPORTED from mint/effects, never duplicated. Reached live via consumer-seed (cognitive_loop.py:93). THE REFUSAL PATH: a failing atom is NOT admitted and its kind="import_reject" record (price + bar + changed/retained/rate) goes through _persist onto the import_queue stream at consumer.py:926 -- the stream that already logs import outcomes; NO new swallow-enum code, no silent drop (count(refusals) == count(records)); idempotent per CANDIDATE RECORD (cand_seq), never per key, so a ctx_min-minimised re-arrival IS re-priced on its RETAINED cells only (context_full never billed) -- arriving tight is how an old atom re-qualifies. THE DIAL: at mint.EXTENT_RATE = 0 admission is byte-identical to the pre-gate door, shipped as a PASSING test. Scope: ONE inequality, ONE refusal path, ONE record -- independence debit, provenance, seed-loading untouched. Gate tests/gate/test_import_gate.py (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 1 build (PROPOSAL_COMPOSER_DESIGN.md §6.1, THE FACTORING; gate tests/gate/test_composer_stage1.py), LOUDLY: rotted BY THIS BUILD -- the COMPOSITE branch inserted at the top of admission_price (~+38 lines: docstring paragraph + the derived-price pricing) moved the seed_imports call site from consumer.py:923 to consumer.py:961, beyond the +/-30 drift tolerance; the refusal _persist moved consumer.py:926 -> 964 and the Gamma write consumer.py:942 -> 980. THE UNPRICED-COMPOSITE HOLE this row's builder stated IS NOW CLOSED where it is closable: a COMPOSITE carrying the compose-time derived signature/price (applicability.csig_of; effects.Gamma.compose stores it under "csig") is priced with the SAME mint inequality -- cost = effects.encoding_cost_atom(changed = sum of leaf changed) + mint.EXTENT_RATE x derived_price, derived_price = start_extent + sum of unguaranteed residues + length, constants still IMPORTED, never duplicated; the returned "retained" carries the derived price so the import_reject record states it verbatim through the UNCHANGED refusal path. An old or underivable composite (no csig) still returns None and passes the door exactly as before -- the hole's residue persists only where nothing derivable exists to price, stated not silent. At mint.EXTENT_RATE = 0 every derivable composite admits: byte-identical to the pre-build door (the dial). Within-tolerance drift noted for accuracy, not rot: admission_price's own def consumer.py:824 -> 825, decline-branch's match def consumer.py:310 -> 311, drain-ranked's consume-loop call consumer.py:767 -> 768, all +1 from the applicability import line; those rows left standing) |
| decline-branch | engines.egocentric.consumer:match | engines/egocentric/consumer.py:285 | LIVE | 2026-08-17 | [helper] the FOURTH BRANCH's tally: the impoverished-description guard at consumer.py:307 routes kind=declined (DECLINE_REASONS enum) via _declined at consumer.py:733/770; reached via consumer-drain. RECEIPT REFRESHED 2026-08-17 (was consumer.py:553, which pointed into _persist_rho_readings and only passed the region check on the substring "match") |
| hydration | cognitive_loop:_hyd_ver | cognitive_loop.py:2106 | LIVE | 2026-08-16 | [helper] A3-1 book-derived verified base; defined cognitive_loop.py:129 (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1661) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1727) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1906) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2029) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2106, drift +37; was cognitive_loop.py:2069) |
| goal-abduction-book | engines.egocentric.goal_abduction:GoalBook | cognitive_loop.py:143 | LIVE | 2026-08-16 | boundary settle via _goal_abd at cognitive_loop.py:120 (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:1244) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1292) |
| goal-abduction-plan | engines.egocentric.goal_abduction:abduced_plan | cognitive_loop.py:1579 | LIVE | 2026-08-16 | verified-atom + site + veto gated; deep plan path (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:1247) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1295) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1426) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1539) |
| goal-abduction-replay-hook | cognitive_loop:CognitiveLoop.bank_replay_levelup | cognitive_game_player.py:1661 | LIVE | 2026-08-18 | [helper] LINK3_AUDIT part (a) -- THE SECOND ROUTE INTO THE ABDUCTION BANK. `_goal_abd` had exactly ONE call site (cognitive_loop.py:1842, inside the cognitive cycle) and `_replay_salient_prefix` never enters that cycle: it steps the environment directly and read `levels_completed` only to classify an outcome. A level-up REACHED BY REPLAY therefore never reached the bank, which is why every banked record in this project is level 1 -- level 1 is reached by exploration (banked), level 2 only by replay (not banked): SUCCESS SUPPRESSES THE EVIDENCE CHANNEL. RECEIPT: the boundary crossing is detected inside the replay step loop and handed to CognitiveLoop.bank_replay_levelup (defined cognitive_loop.py:1456), which shares the ONE banking core `_goal_bank` (cognitive_loop.py:101) with the cognitive cycle -- shared, not duplicated. REACHABILITY: `_replay_salient_prefix` is called from play_game at cognitive_game_player.py:332, BEFORE the explore loop, and `_goal_bank` builds the fabric itself when the cycle has not yet lazily created it (a fresh CognitiveLoop per episode) -- without that the hook is reachable and INERT. OBSERVATION ONLY, exactly as `_ego_feed`: the pre/post frames the environment produced cross the seam, never the replayed prefix (membrane law), and no credit is minted (wheel rule). Containment: never raises into the replay path. Gate tests/gate/test_link3_hook_and_vocabulary.py |
| plan-veto | engines.egocentric.frontier:plan_veto | cognitive_loop.py:1537 | LIVE | 2026-08-16 | B3 frontier veto on the plan site; deep plan path — synthetic coverage floor may not reach it (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:1209) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1252) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:1391) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:1497) |
| harvest | engines.egocentric.frontier:FrontierBook.load_harvest | cognitive_loop.py:2607 | LIVE | 2026-08-16 | FrontierBook constructed cognitive_loop.py:1991; record_harvest cognitive_game_player.py:766 (NOTE REFRESHED 2026-08-17: was :759 — the corpse-guard build added the `terminal` stamp to the step ledger above it). RECEIPTS RE-VERIFIED 2026-08-17 by the DEAD-DEDUP build (all three lines UNMOVED: :2076 load_harvest, :1991 construction, player :766 record_harvest — no refresh needed; the accrual site cognitive_loop.py:2059-2062 was NOT touched). DEAD-DEDUP (PREREG_DEAD_DEDUP.md, audit F-3): the docstring said ">=2 INDEPENDENT RECORDS" and the code counted per LIST ENTRY while the caller banked the per-episode list verbatim — one episode clicking a cell twice blacklisted it forever (MEASURED live ar25 L2: 163 as coded vs 41 as documented, 75% of the blacklist; monotone, nothing decays it). Now counted in DISTINCT RECORDS at BOTH ends: WRITE — record_harvest (engines/egocentric/frontier.py:144-155, the toggle at :35) reduces the dead list to distinct cells in first-seen order, so one episode = one report per cell; READ — load_harvest (engines/egocentric/frontier.py:226-243) counts one report per cell per RECORD, correcting the ~370k of already-banked history AT READ TIME with NO record deleted or rewritten (archive law: evidence is added, never replaced). The >=2 threshold (KNOBS F8) and the effects-outrank-dead rule are UNCHANGED; effects/fatal/deltas are banked verbatim. ABLATION (CLAIM.md's binding clause): env DEAD_DEDUP=0 / module flag frontier.DEAD_DEDUP=False reproduces per-entry counting byte-identically at BOTH ends — the banked bytes equal the literal pre-fix append, and the returned dead set equals the literal pre-fix computation — shipped as PASSING tests. KNOBS G23 (Register G, GUESSED). Gate tests/gate/test_dead_dedup.py (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:2076) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2142) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2360) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2483) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2607, drift +54; was cognitive_loop.py:2553) |
| movement-bias | engines.egocentric.frontier:bias_moves | cognitive_loop.py:4020 | LIVE | 2026-08-16 | B2 bounded bias, never a veto; load_moves cognitive_loop.py:3483; record_moves cognitive_game_player.py:783 (NOTE REFRESHED 2026-08-17: was :776, same shift) (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:3486) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:3552) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:3773) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:3896) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 4020, drift +54; was cognitive_loop.py:3966) |
| salient-prefix | cognitive_game_player:CognitiveGamePlayer._bank_salient_prefix | cognitive_game_player.py:803 | LIVE | 2026-08-17 | [helper] B7 playback channel only; replay via _load_salient_prefix cognitive_game_player.py:325. RECEIPT REFRESHED 2026-08-17 (was :790): the corpse-guard build added the `terminal` stamp at the step ledger (cognitive_game_player.py:422-433), shifting the episode-end bank site by 7 lines (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_game_player.py:797) |
| corpse-guard | cognitive_game_player:CognitiveGamePlayer._corpse_guard_enabled | cognitive_game_player.py:1593 | LIVE | 2026-08-17 | [helper] PREREG_CORPSE_GUARD.md — the guard the MEASURED 13/13 corpse replay needed (FRONTIER_AUDIT F-1: every salient replay ended GAME_OVER, divergence fired ZERO times; the replay was FAITHFUL and reproduced the death). THREE SITES, all reached live: (A) BANK — `terminal` stamped on the step that produced GAME_OVER (cognitive_game_player.py:422-433, live loop) and truncated away in _bank_salient_prefix (cognitive_game_player.py:1545), reached live from the episode-end bank (salient-prefix row, :797); (B) SELECT — this receipt, _load_salient_prefix (:1587) refuses a prefix whose LAST RECORDED OUTCOME was `died` and takes the next alternative, reached live at cognitive_game_player.py:325; (C) THE CONSUMPTION RECORD — _replay_salient_prefix writes outcome + last_used_seq on the prefix row at replay end (cognitive_game_player.py:1677-1697), reached live at cognitive_game_player.py:327. Schema: salient_prefixes gains outcome TEXT + last_used_seq INTEGER (migrated in place, cognitive_game_player.py:1519-1527); NO row is ever deleted (archive law — corpses stay on disk, unselected). ABLATION (CLAIM.md's binding clause): env CORPSE_GUARD=0 / module flag CognitiveGamePlayer.CORPSE_GUARD=False reproduces PRE-GUARD behaviour byte-identically for BOTH banking (the death step banks; prefix_json bytes unchanged — the `terminal` marker is never serialized) and selection (the original ORDER BY uses DESC, created_at ASC LIMIT 1 query, verbatim), shipped as a PASSING test. KNOBS G22 (Register G, GUESSED). NOT ROTATION: the uses-DESC lock-in breaks only as a side effect; rotation is a separate item. Gate tests/gate/test_corpse_guard.py (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_game_player.py:1587) |
| cost-flip | engines.egocentric.latents:ActionCostEstimator | engines/egocentric/planner.py:166 | LIVE | 2026-08-16 | [alias=ESTIMATOR] KNOBS A2: both live plan sites pass cost_per_action=None and consume the estimate; fail-closed under MIN_OBS |
| mastery-lite | engines.egocentric.mastery:MasteryLite | cognitive_game_player.py:107 | LIVE | 2026-08-16 | replay-probability spine in the player |
| cursor-agency | engines.egocentric.agency:CursorAgency | cognitive_loop.py:2673 | LIVE | 2026-08-16 | FIRST ACTIVATION (the movement-stack pair): fed in record_result from pre/post frames + executed action (1-5); consumed by the [NAV] steer via _nav_step_action at cognitive_loop.py:3794; gate tests/gate/test_movement_stack.py (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:2148) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2214) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2426) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2549) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2673, drift +54; was cognitive_loop.py:2619) |
| grid-nav | engines.egocentric.navigation:GridNav | cognitive_loop.py:2673 | LIVE | 2026-08-16 | FIRST ACTIVATION (with cursor-agency): traversability from the agency's map + observed frames in record_result; BFS next-step steers the blind 1-4 draw as a capped bias (NAV_BIAS_P<=0.5, Register G GUESSED), narrated [NAV], never a veto (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:2149) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION build (PREREG_W1_NARRATION.md), LOUDLY: rotted BY THIS BUILD -- the narration helpers _narr_range/_narr_bet/_narr_close (~137 lines) were inserted at module top above the loop class, plus the bet-side call site in cycle and the outcome-side close in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2215) (RECEIPT REFRESHED 2026-08-20 by the W2B PLANNER-SCHEDULING build (PREREG_W2B_PLANNER_SCHEDULING.md), LOUDLY: rotted BY THIS BUILD -- the scheduler helpers _w2b_mark/_w2b_engage/_w2b_abort (~92 lines) were inserted at module top above the loop class, plus the skip-narration branch in _narr_bet, the seam gates + drive stashes in cycle, and the abort-router call site in record_result, moving this site further than the +/-30 drift tolerance; was cognitive_loop.py:2426) (RECEIPT REFRESHED 2026-08-20 by the W1 NARRATION-ARMS build (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"), LOUDLY: rotted BY THIS BUILD -- the arm-switch init in start_game (+12), the arm narration + per-step stash clears in _narr_bet/_narr_close (+20), the replay-seam arm label in _ego_feed (+6), and the WIRE 1 (route tie-break) / WIRE 2 (mint guard-zero skip) consumption call sites in record_result (+~32) moved this site further than the +/-30 drift tolerance; the consumption helpers themselves were appended at MODULE BOTTOM precisely so the helper block moved nothing; was cognitive_loop.py:2549) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2673, drift +54; was cognitive_loop.py:2619) |
| rho-ladder | engines.egocentric.rho:rho_report | engines/egocentric/consumer.py:558 | LIVE | 2026-08-17 | S8 FLIPPED (was WIRE FIRST): consume() persists per-pass {r0,r1,r2,traffic} per matched source to collective "rho_readings" (consumer._persist_rho_readings, called consumer.py:780) via rho_report — RECEIPT REFRESHED 2026-08-17 (was consumer.py:480/700): the ranked-drain build inserted ~73 lines above it and the rot-guard fired exactly as designed — which drives the whole trio, rho_at (rho.py:174) at rungs 0/1/2 + rederivation_traffic (rho.py:190) both ways, on live books; [RHO] line narrates r0/r2/traffic; stream consumer = the beat protocol (THE_LADDER rung 4); gate tests/gate/test_rho_ladder_live.py |
| applicability-index | engines.egocentric.applicability:prune_candidates | engines/egocentric/planner.py:226 | LIVE | 2026-08-20 | W2a STAGE 1 (PREREG_W2_APPLICABILITY_INDEX.md): the applicability pre-filter over Gamma. WRITE SITE: mint.py:320 stamps applicability.anchor_signature (context dims + palette set + content key) on the atom BEFORE Gamma.add (NOTE-REFERENCE REFRESHED 2026-08-20 by the W2 STAGE 2 CONTEXT-MIN build (PREREG_W2_STAGE2_CONTEXT_MIN.md), LOUDLY: rotted BY THIS BUILD -- the EXTENT_RATE constant block, the docstring additions and the intersection-at-rederivation call were inserted above it in mint.py; was mint.py:281; a SECOND stamp site now exists at mint.py:369 inside _intersect_context, restamping after minimisation so the cache never goes stale; the gate-checked READ SITE planner.py:226 did not move) (NOTE-REFERENCE REFRESHED AGAIN 2026-08-20 by the RE-POINT AMENDMENT build (PREREG_W2_STAGE2_CONTEXT_MIN.md, THE RE-POINT AMENDMENT), LOUDLY: rotted BY THIS BUILD -- the coarse-signature index state (CONFLICT_SCAN_CAP, __init__ maps) and the _signature_merge call in consider() were inserted above the mint-time stamp, which is now mint.py:356 (was mint.py:320, beyond the +/-30 drift tolerance); the restamp site moved into the renamed _merge_context at mint.py:468 (was mint.py:369 in the deleted _intersect_context); a THIRD stamp site now exists at mint.py:553 inside _reinstate -- the conflict clause restamps after reinstating cells from context_full, same cache-never-stale rule; the gate-checked READ SITE planner.py:226 did not move) (NOTE-REFERENCE REFRESHED A THIRD TIME 2026-08-20 by THE VERDICT REASON FIELD build (W4: every mint verdict names its deciding clause; gate tests/gate/test_verdict_reason.py), LOUDLY: rotted BY THIS BUILD -- ~92 lines of reason machinery (docstring paragraph, REASON_* vocabulary, _record parameters, _mdl_failed) were inserted above the stamp sites in mint.py: the mint-time stamp is now mint.py:448 (was mint.py:356), the _merge_context restamp mint.py:562 (was mint.py:468), the _reinstate restamp mint.py:647 (was mint.py:553); the gate-checked READ SITE planner.py:226 did not move); every pre-field atom is DERIVED ON READ (signature_of backfill -- the index is derived state, rebuildable from the atoms stream, never the sole holder). READ SITE (this receipt): plan_to_identity reads frame_signature ONCE per call and prunes the candidate set BEFORE any apply_effect -- O(1) set/dim comparisons per atom, palette closure over introduced colours, inverse-path aware in reference mode. CONSERVATIVE (F2 absolute): an atom that CAN apply is never pruned; unknown ttypes/kinds/composites are kept. No change to apply_effect semantics or plan legality -- only WHICH candidates are evaluated. UNDO = remove the consult; the search reverts to exhaustive. Gate tests/gate/test_applicability_index.py (NOTE-REFERENCE REFRESHED 2026-08-21 by the COMPOSER STAGE 1 build (PROPOSAL_COMPOSER_DESIGN.md §6.1, THE FACTORING; gate tests/gate/test_composer_stage1.py): every asig stamp site now stamps the POSTCONDITION SIGNATURE (applicability.PSIG_FIELD, the postcondition-signature row below) BESIDE it -- the mint-time stamp mint.py:448 (unchanged) with the psig stamp at mint.py:458, the _merge_context restamp mint.py:562 -> 571 with psig at 574, the _reinstate restamp mint.py:647 -> 658 with psig at 661 (+9..+14, all WITHIN the +/-30 drift tolerance -- noted for accuracy, not rot); AND the COMPOSITE branch of _derive now returns a compose-time stored derived precondition (csig, the composite-signature row below) instead of unconditional NO-REQUIREMENT, so derivable composites are index-visible and pruneable -- underivable/legacy composites still degrade to NO-REQUIREMENT, never pruned (F2 conservatism unchanged); the gate-checked READ SITE planner.py:226 did not move) |
| narration-spine | engines.egocentric.narration:NarrationSpine | cognitive_loop.py:270 | LIVE | 2026-08-20 | W1 (PREREG_W1_NARRATION.md): the narration spine -- bet-side records (BET/PLAN/ACT) emitted in cycle() BEFORE the action executes via _narr_bet (constructed cognitive_loop.py:270); outcome-side records (PERCEIVE/ROUTE/MINT/ECHO) close against the bet by reference in record_result via _narr_close; replay steps narrate [REPLAY] in _ego_feed (this receipt: the cycle-seam construction at :270; the _ego_feed replay-seam twin at :1628 shares the same spine instance). PERSONAL "narration" stream, one JSONL record per event, O(1) per emit; every record carries a memory range ([EP]/[OWN]/[COL] with contact-class provenance, [REPLAY] for playback). Consumer = the falsifier experiment (allowlisted in tests/gate/test_consumers.py until it lands). Gate tests/gate/test_narration_spine.py  (RECEIPT REFRESHED 2026-08-20 by the W1 narration build, LOUDLY: bet-before-act insertions moved sites; was :1628) |
| planner-scheduler | engines.egocentric.scheduler:PlannerScheduler | cognitive_loop.py:399 | LIVE | 2026-08-20 | W2b (PREREG_W2B_PLANNER_SCHEDULING.md): THE PLANNER AS LAST RESORT -- the scheduling decision, separate from the W2a index (the index makes the search cheaper; this decides WHEN it is worth running; evidence: planner calls scaled 5->10->17 with freed capacity across three profiling windows, F2 addendum). Constructed lazily in _w2b_engage (cognitive_loop.py:399), the ONE engagement decision consulted by BOTH plan seams in cycle() -- the reference-mode plan_to_identity seam (cognitive_loop.py:1458) and the abduced-goal seam (cognitive_loop.py:1545). GATE A (cheap routes first): cf.action_confidence at or above scheduler.CHEAP_ROUTE_CONF_BAR=0.5 (KNOBS G25, Register G, GUESSED -- mirrors the loop's exploit-certainty bar) holds the wheel, no search. GATE B (no re-search of an unchanged world): retained (state key, mint/import mark) per (game, level); identical -> SKIP, narrated at the PLAN point with its reason (mode=skipped, gate=reason) via _narr_bet -- never silent. STARVATION GUARD (absolute): conf below the bar + no retained identical attempt opens BOTH gates by construction, and the loop seam FAILS OPEN on any internal error -- a starved cycle always reaches the planner in that same cycle. Level change or fission clears the retained keys (binder.on_level_change's pattern; clear site cognitive_loop.py:439 in _w2b_abort). state_key = the planner's OWN planner._state_key, so scheduler and search agree on what unchanged means. UNDO: remove the two seam conditions; the planner reverts to engage-on-atoms. Gate tests/gate/test_planner_scheduling.py (F2/F3/F4/R4; F1 is the proctor's live profile read) |
| context-min | engines.egocentric.effects:minimise_atom | engines/egocentric/mint.py:590 | LIVE | 2026-08-20 | W2 STAGE 2 + THE RE-POINT AMENDMENT (PREREG_W2_STAGE2_CONTEXT_MIN.md): CONTEXT MINIMISATION -- the pi-replay null's repair (PI_REPLAY_RESULT.md: median context 976 cells licensing a 26-cell change; nothing can match anything but its own source frame). THE WIRE (this receipt): MDLMint._signature_merge -> _merge_context calls effects.minimise_atom (mint.py:590) at EVERY well-formed changed consider() whose COARSE SIGNATURE (mint._signature -- change-only, deliberately debris-blind; support already accrues on it) matches a minted atom, inside the LIVE mint-core path. THE TRIGGER IS THE COARSE SIGNATURE, NOT THE FULL ATOM KEY: full-key collisions carry byte-identical contexts, so the previous full-key wire was structurally vacuous exactly as its own STRUCTURAL FACT note stated -- the re-point makes different-debris re-observations of the same change reach the same atom. Mapping signature -> atom id is IN-MEMORY per process (the _seen pattern) AND lazily re-derived from Gamma (_refresh_sig_index / _atom_signature reproduce the live signature from stored patches, stable under minimisation), so a restarted worker re-links on its first consider(). Mechanics unchanged: varying cells become effects.DONT_CARE (-1), changed cells + one 8-ring ALWAYS retained, monotone shrink, context_full preserved on first touch (the undo; CONDITION 2: nothing may delete it), update = SUPERSEDING APPEND on the atoms stream (same id, ctx_min marked; Gamma.get is last-wins). THE CONFLICT CLAUSE (CONDITION 1, mint._conflict_clause, bounded: CONFLICT_SCAN_CAP=64 minimised atoms per call, applicability-signature pre-screen before any anchor scan): an observation whose signature DIFFERS but whose before-frame matches a minimised atom's loosened context REINSTATES the distinguishing cells from context_full with the atom's originally observed values, PINS them (ctx_conflict_cells; effects.minimise_atom keeps pins -- divergence tightens, never loosens), record marked ctx_conflict. THE PREMIUM rides unchanged: mint.EXTENT_RATE (0.05, arithmetic in its docstring) prices retained-but-unchanged context cells inside the MDL inequality (mint.py:409, effects.context_retained_cells); EXTENT_RATE=0 is byte-identical to the pre-premium decisions (the dial); the half-board clause stays as the outer wall. Match-path support: DONT_CARE-aware anchor scans + masked stamp in effects.py, scalar path the semantics oracle, vectorised equivalence gated. UNDO: EXTENT_RATE=0 + remove the one _signature_merge call in consider(); context_full restores originals. Gate tests/gate/test_context_min.py (+ the extended corpus in tests/gate/test_vectorised_scan.py) (RECEIPT REFRESHED 2026-08-20 by the RE-POINT AMENDMENT build (PREREG_W2_STAGE2_CONTEXT_MIN.md, THE RE-POINT AMENDMENT), LOUDLY: rotted AND re-pointed BY THIS BUILD -- _intersect_context was replaced by the coarse-signature machinery (_atom_signature/_refresh_sig_index/_signature_merge/_merge_context/_conflict_clause/_reinstate) and the minimise_atom call site moved beyond the +/-30 drift tolerance; was engines/egocentric/mint.py:362, trigger was the full atom key at rederivation of a known key) (RECEIPT REFRESHED 2026-08-20 by THE VERDICT REASON FIELD build (W4: every mint verdict names its deciding clause -- "reason" from the fixed REASON_* vocabulary, "failed" = every independently failing clause in the code's own evaluation order, extent_premium attributed by re-evaluating the SAME clauses at the rate-0 cost via _mdl_failed, audit numerics under "mdl"; verdict names, flat "w" emission sites and all prior fields byte-identical -- additive only; gate tests/gate/test_verdict_reason.py), LOUDLY: rotted BY THIS BUILD -- the module-docstring reason paragraph (~+16), the REASON_* vocabulary constants (~+26), the _record reason/failed/changed/mdl parameters + the _mdl_failed helper (~+38) and the per-terminal reason stamping in consider() (~+14) were inserted above the minimise_atom call site, moving it beyond the +/-30 drift tolerance; was engines/egocentric/mint.py:461; the premium term moved to mint.py:409 (was mint.py:330)) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 2 build (PREREG_COMPOSER_STAGE2_ENABLES.md: the ENABLES indices + the act_offset stamp), LOUDLY: rotted BY THIS BUILD -- the enables import (+1), the consider() act= parameter + its convention comment (~+5) and the ACT-RELATIVE OFFSET stamp block beside psig (~+19) were inserted above the minimise_atom call site, moving it beyond the +/-30 drift tolerance (the row stayed accidentally green only because the _merge_context docstring MENTIONS minimise_atom inside the old window -- a mention is not a receipt); was engines/egocentric/mint.py:554 (the call is now mint.py:590); the premium term moved to mint.py:415 (was mint.py:409)) |
| abort-router | engines.egocentric.scheduler:route_abort | cognitive_loop.py:446 | LIVE | 2026-08-20 | W2b RIDER (the shadow test's Interruption finding): a driven plan's mid-execution abort is ROUTED, never conflated -- WORLD-MOVED (state key changed under the plan: re-plan, NO penalty; the retained GATE-B key drops so the re-plan is not blocked) vs PLAN-WRONG (state as predicted, step failed: recorded against the plan's atoms in PlannerScheduler.plan_wrong -- a ledger for the eviction gap, deliberately not yet a demotion). Both narrated at the PLAN point with their discriminator (mode=abort, gate=route). Consumed in _w2b_abort (one-line call site in record_result, cognitive_loop.py:2161) from the drive-time stash _w2b_driven (both drive sites stash plan-time key + step atoms: cognitive_loop.py:1523, cognitive_loop.py:1582). Today a driven plan is a single same-cycle step, so live aborts compare equal keys and route PLAN-WRONG; WORLD-MOVED is the seam a multi-cycle drive inherits, gate-tested via constructed calls. Gate tests/gate/test_planner_scheduling.py (F4) (PROMISE FULFILLED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build: the multi-cycle drive the note anticipated now exists -- a driven COMPOSITE chain issues one part per cycle; _w3d_continue routes WORLD-MOVED through route_abort on a state-key change between parts (cognitive_loop.py:4998), _w3d_settle routes PLAN-WRONG on the MISPREDICTING component only (cognitive_loop.py:5064) with ctx_conflict via the mint's reinstate path; each part rides the SAME _w2b_driven stash (key + steps). Site unchanged: drift +23, within tolerance) |
| narration-arm-switch | engines.egocentric.narration:NarrationSpine.narrate_arm | cognitive_loop.py:277 | LIVE | 2026-08-20 | W1 FALSIFIER ARMS (PREREG_W1_NARRATION.md, "ARM C's CONSUMPTION MUST BE REAL"): the arm switch -- NARRATION_ARM env read ONCE at game init (start_game cognitive_loop.py:825, narration.resolve_arm; only an explicit C consumes, anything else is W = narrate-only, today's exact behaviour, the default) and narrated ONCE per game as the ARM record (point=ARM, side=meta, consume flag) on the personal narration stream at BOTH entry seams: the first bet in _narr_bet (this receipt, :277) and the replay-first twin in _ego_feed (:1790) -- so the shared-falsifier experiment's episode->arm labeling is read FROM DATA, never inferred. Idempotent per spine (= per game); loops with no arm state (constructed harnesses) emit no ARM record and keep today's record shape. Gate tests/gate/test_narration_arms.py |
| narration-arm-route | cognitive_loop:_narr_expected_bin | cognitive_loop.py:2192 | LIVE | 2026-08-20 | [helper] W1 FALSIFIER WIRE 1 (arm C ONLY): ROUTE consumes the immediately-prior BET narration record's stated expected bin for the slot, read from NarrationSpine.last_bet IN-MEMORY (never the JSONL on the hot path), passed as expected_bin at the ONE routing call site in record_result (this receipt). The router consults it ONLY inside router.AMBIGUOUS_BAND of its eps threshold (the discriminating fact's own tie region -- the WIRE 1 decision point, engines/egocentric/router.py:93; band = KNOBS G27); a consumed tie-break cites the consumed BET id on the step's ROUTE narration (provenance). Arm W passes None ALWAYS: the consumption branch is structurally unreachable and router.consumed stays 0 (gate-asserted). Helper defined at cognitive_loop.py:4560 -- MODULE BOTTOM, deliberately, so the helper block moves no existing receipt line. Gate tests/gate/test_narration_arms.py (the wire check: one borderline residual routes BOTH ways on the prior bet's presence alone) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2192, drift +37; was cognitive_loop.py:2155) |
| narration-arm-mint | cognitive_loop:_narr_mint_skip | cognitive_loop.py:2286 | LIVE | 2026-08-20 | [helper] W1 FALSIFIER WIRE 2 (arm C ONLY): MINT consumes its own guard-zero history -- a transition signature whose LAST MINT narration record named a guard as the zero, with that guard's input unchanged (support count same, offer route same; NarrationSpine.last_mint in-memory, LAST_MINT_CAP=128 = KNOBS G28), is NOT re-offered this cycle; the skip is itself narrated at the MINT point with reason "guard-zero unchanged" + the consumed record's id (provenance). Consulted at ALL THREE offer sites in record_result (bootstrap :2235 this receipt, queue :2256, primal :2287); retention via _narr_mint_mark after every real offer (:2247, :2268, :2298). Arm W returns False before touching anything: re-offers every cycle, spine.last_mint stays empty, no narration record grows a field -- byte-identical to today (gate-asserted). Helpers at cognitive_loop.py:4611/4652, MODULE BOTTOM. Gate tests/gate/test_narration_arms.py (the wire check: the identical third offer is withheld in C -- mint support state frozen, no verdict ledgered -- and made in W) (RECEIPT REFRESHED 2026-08-21 by the COMPOSER STAGE 4 SETTLEMENT build (PREREG_COMPOSER_STAGE4_SETTLEMENT.md), LOUDLY: rotted BY THIS BUILD -- the PLAN-record extra carrier in _narr_bet (+4), the chain-continuation seam _w3d_continue at the top of cycle's plan block (+10), the two _w3d_drive calls at the compose seams (+8, +4) and the _w3d_settle call site in record_result (+3) moved this site further than the +/-30 drift tolerance on top of the drift prior builds had already consumed (this build alone added +26/+29 here); the stage-4 helpers _w3d_site/_w3d_step/_w3d_narrate/_w3d_abort/_w3d_drive/_w3d_continue/_w3d_settle (~290 lines) were appended at MODULE BOTTOM precisely so the helper block moved nothing; measured `grep -n` -> 2286, drift +51; was cognitive_loop.py:2235) |
| postcondition-signature | engines.egocentric.applicability:postcondition_signature | engines/egocentric/mint.py:458 | LIVE | 2026-08-21 | [helper] COMPOSER STAGE 1 deliverable 1 (PROPOSAL_COMPOSER_DESIGN.md §6.1; COMPOSITION_VIA_SIGNATURES.md Q2 -- "the postcondition derives at mint time, and most of it already exists in sigma"): the atom's POSTCONDITION SIGNATURE, factored beside anchor_signature from what the atom already stores -- after-patch dims, colours WRITTEN (after values at changed cells; sigma.colour_delta where patches are unreadable), changed count + the changed-mask content key (the mint's _signature hashing shape in a pure-python encoding), palette-after (= before - consumed, union written, at the patch grain). WRITE SITES (this receipt, the mint-time stamp): mint.py:458 in consider(), BESIDE the asig stamp at mint.py:448; restamped at the same two supersede sites asig restamps (_merge_context mint.py:574, _reinstate mint.py:661 -- the cache never outlives its patches; effects.minimise_atom drops both stale caches). READ SIDE: applicability.psig_of -- stored stamp when valid, DERIVED on read for every atom that predates the field (backfill-on-read, never a migration; the stored copy is a cache). Same total/never-raises discipline as asig: anything unreadable degrades to NO-CLAIM (writes nothing -- no edge, no discount, never a false claim); DONT_CARE never leaks into any field (a minimised cell is unchanged by construction). Named consumers: the composite derivation below today; the ENABLES-edge index (stage 2) is the stated next consumer. Gate tests/gate/test_composer_stage1.py (R4 + the three-site stamp test) |
| composite-signature | engines.egocentric.applicability:composite_signature | engines/egocentric/effects.py:1054 | LIVE | 2026-08-21 | [helper] (NOTE REFRESHED by COMPOSER STAGE 4.5: the "three consumers" are now all REAL -- (3) is the composer's LOCAL MINT paying consumer.extent_bargain on the derived price before Gamma.compose (admission-bargain row), and a chain with NO derivable signature is refused there (csig-underivable), never inf-ranked. Repair 6: the PALETTE credit is now single-prior like the CELL-COUNT credit (_established returns the establishing prior; only its writable universe is subtracted) -- the pricing rule is UNCHANGED, and _derive_composite's docstring states that multi-step chains are OVERPRICED BY DESIGN pending the multi-prior union credit.) COMPOSER STAGE 1 deliverable 2 (PROPOSAL_COMPOSER_DESIGN.md §6.1; COMPOSITION_VIA_SIGNATURES.md Q5 -- "the composite's precondition derivation IS the composite's price derivation: one computation, three consumers"): at compose() time (this receipt -- effects.Gamma.compose, effects.py:1054, lazy import to avoid the applicability->effects cycle) the composite's parts are FLATTENED to leaf EFFECT steps (nesting derives through the recursion, exactly Gamma.apply's execution order, depth-capped) and ONE derivation yields (a) the derived PRECONDITION signature -- step 1's minimised context requirement (dims = max over leaves, frame dims being apply-invariant; palette = step 1's + each later step's residue colours minus prior steps' writable universe) -- and (b) the derived PRICE = start_extent + sum of unguaranteed residues + length, where a later step's UNGUARANTEED RESIDUE = its non-DONT_CARE context cells no single prior step's stored after-patch establishes under best full-containment alignment (position-free, the only guarantee stored patches can state; multi-prior unions NOT credited -- doubt overprices admission and under-constrains pruning, never the reverse). Stored on the composite atom under applicability.CSIG_FIELD ("csig"); read via applicability.csig_of (validated, no backfill -- no resolver reaches the read side). CONSUMERS TODAY: (1) applicability._derive's COMPOSITE branch returns the stored precondition -- composites index-visible and pruneable via the applicability-index row's unchanged planner call site; (2) consumer.admission_price prices composites by the stored derived price (import-admission-gate row) -- the unpriced-composite hole closes; (3, stated) the shadow gate's PAY. Underivable (missing/EFFECT_IF/INERT parts, any doubt) -> field ABSENT: NO-REQUIREMENT, unpriced, never pruned -- byte-identical to the pre-build composite everywhere. Gate tests/gate/test_composer_stage1.py (F1 chain discount derived-executable, F2 index visibility, F3 admission, F4 nesting) |
| act-offset-stamp | engines.egocentric.enables:act_offset | engines/egocentric/mint.py:482 | LIVE | 2026-08-21 | COMPOSER STAGE 2 deliverable 2 (PREREG_COMPOSER_STAGE2_ENABLES.md, "THE REPRESENTATION CHOICE, NAMED"): THE ACT-RELATIVE OFFSET -- act_offset = act cell - matched anchor origin, stamped by mint.consider() BESIDE psig at the same write site (mint.py:482) when the minting action carried coordinates. The anchor origin at mint time IS the changed-cell bbox origin (learn_effect crops the context patch there, so the patch matches the before-frame at exactly that cell). THE CAPTURE (the derivability verdict's one insertion, authorized by the brief): cognitive_loop.py:2224-2235 builds _wact = (row, col) = (y, x) from _last_action_info (the loop's click convention is (x, y) = (col, row) -- spine.remap_avoided's contract; frames index [y, x], cognitive_loop.py:3043) and the three mint offer sites pass act=_wact (consider() calls at cognitive_loop.py:2249/2271/2302 -- bootstrap, queue, primal). NO CONTENT BACKFILL EXISTS, stated: unlike asig/psig the offset is not a function of the atom's stored patches (checked on live boxes 2026-08-21: no atom record carries coordinates; the action_traces DB does carry a coordinates column, so an OFFLINE join-instrument could one day backfill history, but that is a tool, not a read side) -- enables.act_offset_of serves the stored stamp or None, absence permanent and stated, never approximated (the prereg's "absent otherwise"). Survives ctx_min/ctx_conflict supersedes by construction (both preserve the patch bbox; the offset is anchor-relative -- asserted in the gate). Gate tests/gate/test_composer_stage2.py |
| route-ambiguous-band | engines.egocentric.router:AMBIGUOUS_BAND | engines/egocentric/router.py:93 | LIVE | 2026-08-20 | [helper] W1 FALSIFIER WIRE 1's NAMED BAND (KNOBS G27, Register G, GUESSED): the tie region around the residual router's own eps threshold (1e-9; the residual<=eps discriminating fact) inside which -- and ONLY inside which -- arm C's expected_bin resolves TRANSFERRED-vs-neighbour. Derivation: every bank residual is a whole count (changed cells; Manhattan centroid steps -- bank.py), so the smallest residual the substrate can testify to is 1.0 and 0.5 is half of it: below that the bare rule is a float-noise coin toss. Consumption site = the one guarded branch in ResidualRouter.route (this receipt); arm W (expected_bin=None) never enters it at any band value. Gate tests/gate/test_narration_arms.py |
| composer-enables-edges | engines.egocentric.enables:enables_edges | engines/egocentric/composer.py:502 | LIVE | 2026-08-21 | (SITE REFRESHED 319 -> 502 by COMPOSER STAGE 4.5, the seam repairs: the attempt body grew above _enablers_of -- the want-already-satisfied guard, the avatar-colour read, the reconciled-anchor proposals; the edge's consumer and semantics are unchanged. Stated, not hidden.) FLIPPED SEVERED -> LIVE 2026-08-21 by COMPOSER STAGE 3 (THE COMPOSE LOOP, PROPOSAL_COMPOSER_DESIGN.md par.3/6.3): the stage-2 stated promise is FULFILLED by its named consumer -- composer.compose_attempt computes the within-Gamma edge (lazily, once per attempt, over the game's pool with the frame signature) to supply a one-step enabler A for a WANT-writing candidate B that has NO matching anchor in the current frame: chain [A, B], simulated seam by seam, minted as a CANDIDATE composite (this receipt: engines/egocentric/composer.py:319, inside _enablers_of). The compose loop itself runs ONLY at cognitive_loop's two W2b-gated plan seams and ONLY on planner-None (see composer-loop row). Was SEVERED at the wire-break line enables.py:132 (stage 2 shipped the instrument ahead of its consumer, discipline as action-book); the edge semantics are unchanged from that row: missing_B = required_B - frame_palette, edge iff missing_B nonempty and intersecting written_A, stored signatures only, doubt produces absence, empty Gamma -> empty graph. Gates tests/gate/test_composer_stage2.py (the edge) + tests/gate/test_composer_stage3.py (the consumer, R4 enabler chain) |
| composer-cross-shelf-reach | engines.egocentric.enables:cross_shelf_reach | engines/egocentric/composer.py:541 | LIVE | 2026-08-21 | (SITE REFRESHED 344 -> 541 by COMPOSER STAGE 4.5, record/findings/COMPOSER_SEAM_READ.md: the verified=False CONTRACT IS NOW READ at the consumer, not merely honoured by construction -- reach["verified"] is not False refuses the WHOLE attempt with reach-contract-breach (tests/gate/test_composer_stage45.py H); the reach's "anchor" and "target" are carried on the proposal so reach, simulation stamp and drive click site name ONE cell (no anchors[0] anywhere); a prefix the plan-time frame already makes unnecessary -- the act cell already wears the avatar's colour -- is dropped for the shorter chain (prefix-unnecessary), a needed one is stated prefix-required and its AVATAR-AT-ACT-CELL CHECK must pass.) FLIPPED SEVERED -> LIVE 2026-08-21 by COMPOSER STAGE 3 (THE COMPOSE LOOP, PROPOSAL_COMPOSER_DESIGN.md par.3/6.3): the stage-2 stated promise is FULFILLED by its named consumer -- composer.compose_attempt calls the reach for every WANT-writing candidate whose act cell (matching anchor + act_offset) is not the avatar's current position, taking the returned BODY chain as the movement prefix (this receipt: engines/egocentric/composer.py:344). THE verified=False CONTRACT IS HONOURED DOWNSTREAM: the consumer re-simulates every proposed chain seam by seam (apply_effect + the per-step ENDPOINT CHECK) and an unverifiable chain is never minted -- the algebra proposes, the simulation decides, exactly the flag's stated meaning. Delta source at the live seam = enables.book_deltas (the stated primary); mask = enables.fatal_cells (the one conversion point), both assembled in cognitive_loop._w3c_compose. Was SEVERED at the wire-break line enables.py:178. Gates tests/gate/test_composer_stage2.py (the reach) + tests/gate/test_composer_stage3.py (F1 two-shelf, F4 honesty, known-negative unreachable) |
| composer-loop | engines.egocentric.composer:compose_attempt | cognitive_loop.py:4795 | LIVE | 2026-08-21 | (SITE REFRESHED 4778 -> 4795 by COMPOSER STAGE 4.5 -- THE SEAM REPAIRS, record/findings/COMPOSER_SEAM_READ.md, gate tests/gate/test_composer_stage45.py: _w3c_compose now passes the self-locus EXACT cell (observer "cell" -> loop._ego_self_cell, token self-cell; centroid rounding is the STATED fallback, token centroid-rounded, both on the PLAN record) and stamps the avatar source on the result. compose_attempt: (1) refuses a WANT the frame already holds BEFORE reading the pool (want-already-satisfied) and requires a predicate FLIP against the plan-time baseline; (2) refuses csig-less chains (csig-underivable, never inf-ranked) and PRICES every local mint by consumer.extent_bargain before Gamma.compose (price-refused with the price -- the admission-bargain row); (3) stamps every Gamma seam AT THE RECONCILED ANCHOR (_apply_at) and carries per-part anchors ("anchors") for the drive; an offset-less atom composes as a no-movement chain only (notes no-act-offset); (4) runs the AVATAR-AT-ACT-CELL CHECK after a BODY prefix and prefers the shorter chain where the prefix contributed nothing (prefix-unnecessary / prefix-required). Every outcome still a fixed token; "refused" and "notes" state the per-chain refusals.) COMPOSER STAGE 3 (PROPOSAL_COMPOSER_DESIGN.md par.3/6.3; the build after PREREG_COMPOSER_STAGE2_ENABLES.md): THE COMPOSE LOOP -- WANT -> Gamma candidates by psig-written intersecting the WANT's target colours (stored signatures, the enables edge logic pointed at the goal) -> anchors via the existing vectorised matcher (effects._context_anchors) -> BODY prefix via cross_shelf_reach / enabler prefix via enables_edges -> chains simulated seam by seam (apply_effect; movement exact ONLY through minted TRANSLATE atoms + the per-step endpoint check, anything outside that scope proposed-unverified and NEVER minted) -> verified chains ranked by the DERIVED price (applicability.composite_signature, the stage-1 derivation's third consumer) -> cheapest -> Gamma.compose() -> COMPOSITE minted as CANDIDATE (citability is STAGE 4's settlement wire; nothing marks it citable). THE ONE CALL SITE (this receipt): cognitive_loop._w3c_compose (module bottom, cognitive_loop.py:4752), reached ONLY from cycle()'s two plan seams (reference diff cells at cognitive_loop.py:1572; abduced predicate at cognitive_loop.py:1640), each INSIDE the open W2b gate (_w2b_engage True -- no new engagement, the scheduler's leak-stop survives, gated by AST containment) and ONLY when the planner's search returned None (the fallback ordering). Outcome narrated at the PLAN point: mode "composed" + composite id, or "compose-none" + the fixed reason token. Design par.5 held: no new search, no authored semantics, candidates only. Gate tests/gate/test_composer_stage3.py (F1/F2/F3/F4/F5/R4/known-negatives) (STAGE 4 LANDED 2026-08-21 -- the settlement wire, PREREG_COMPOSER_STAGE4_SETTLEMENT.md: _w3c_compose now RETURNS the attempt's result and stages the composite's citation state (settled False) on the PLAN record; the CANDIDATE may then DRIVE via _w3d_drive at both compose seams (cognitive_loop.py:1589, :1659) under the planner's own verified/site/veto gates, incrementing THE SAME g7 on self._plan_gate. SITE REFRESHED 4752 -> 4778 (the compose_attempt call moved +26 by the stage-4 insertions above; the gate still passed on the docstring mention at 4751 -- refreshed to the true call line, stated not hidden)) |
| composer-settlement | engines.egocentric.composer:live_settle | cognitive_loop.py:5153 | LIVE | 2026-08-21 | (SITE REFRESHED 5073 -> 5153 by COMPOSER STAGE 4.5, the seam repairs: the drive seam grew -- _w3d_drive shadows an offset-less composite with no-act-offset on the PLAN record (never driven from a synthesised site: _w3d_site takes the RECONCILED anchor per part from the drive record, the patch-centre convention is gone, a stale anchor yields no site), narrates a malformed drive record (drive-record-malformed) and carries avatar/prefix on every composite PLAN record; _w3d_continue narrates could-not-issue. drive_record refuses want_cells=None without a predicate and a predicate already true on frame0; settle_verdict re-verifies the predicate FLIP on the LIVE frame (predicate-unmet -- want_cells=None never means "no check"). is_citable / citation_allowed / citation_state read the STREAM RECORD through gamma's record access (an id + gamma), refusing gamma.get's atom shape loudly (not-a-stream-record) so `settled` is readable in production. Gate tests/gate/test_composer_stage45.py S1/S3/H/R4.) COMPOSER STAGE 4 (PREREG_COMPOSER_STAGE4_SETTLEMENT.md; after stage 3 a494cda): THE SETTLEMENT WIRE -- ONE transition, CANDIDATE -> SETTLED, written ONLY by composer.live_settle (a superseding append, same id, settled: True on the envelope -- Gamma's supersede idiom; the candidate record stays readable history) after composer.settle_verdict compared the chain's predicted final frame (stage 3's simulation, carried in the drive stash) against the LIVE frame on the WANT cells and every cell the chain predicted. Simulation never settles (structural gate: one writer, and it calls the live comparison). THE ONE call site: _w3d_settle in record_result (this receipt), placed BEFORE _w2b_abort so a composite stash is consumed by its own seam and the planner's passes through untouched. The drive: _w3d_drive (cognitive_loop.py:4906) under the planner's own gates (verified x2-TRANSFERRED per part via _atom_verified, site, frontier plan_veto) -- g7 AND drive increment on self._plan_gate, the SAME dict the plan_to_identity path increments (F4, identity-asserted; no parallel counter); the continuation: _w3d_continue (cognitive_loop.py:4974) at the top of cycle's plan block, one part per cycle, ONLY on an equal state key. FAILURE ROUTES THROUGH EXISTING MACHINERY ONLY: scheduler.route_abort -> WORLD-MOVED (candidate intact, no penalty, GATE-B key dropped) vs PLAN-WRONG (ctx_conflict on the mispredicting component -- the first part whose own predicted cells diverged -- via composer.conflict_component -> MDLMint._reinstate, cognitive_loop.py:4895; PlannerScheduler.plan_wrong ledgers that component only). Nothing deleted. Citation discipline: composer.is_citable / citation_allowed (pure; see the composer-citation-rule row). Gate: tests/gate/test_composer_stage4.py (F1-F4, R4, known-negatives, structural one-writer) |
| composer-citation-rule | engines.egocentric.composer:citation_allowed | engines/egocentric/composer.py:867 | SEVERED | 2026-08-21 | [instrument] (BREAK LINE REFRESHED 574 -> 867 by COMPOSER STAGE 4.5: the rule now reads the STREAM RECORD through gamma's record access -- citation_allowed(id, role, gamma=...) / is_citable(id, gamma=...) / citation_state(gamma, id) -- so its named consumer can actually read `settled` in production; gamma.get's atom shape is refused loudly (not-a-stream-record) rather than misread as never-citable. Still SEVERED: the consumer has not landed. Gate tests/gate/test_composer_stage45.py H.) COMPOSER STAGE 4 shipped THE CITATION RULE as a pure function (unsettled composites may appear in a BET, never as GROUND; settled flips exactly GROUND -- is_citable reads the settled flag only live_settle writes) with its STATED CONSUMER named and not yet wired: the stage-1 shadow gate's BET/GROUND check (PROPOSAL_COMPOSER_DESIGN.md par.6.4 / amendment 4 -- the composer's utterances ARE the gate's composition requests). Severed BY DESIGN until that consumer lands (stage 2's idiom: the instrument ships with its consumer named); the inverse-reference assert guards a silent reconnection. The wire-break line is the def itself. Gate: tests/gate/test_composer_stage4.py F2 |
| admission-bargain | engines.egocentric.consumer:extent_bargain | engines/egocentric/composer.py:605 | LIVE | 2026-08-21 | COMPOSER STAGE 4.5 (record/findings/COMPOSER_SEAM_READ.md, silent #2 -- "admission_price is reached only from the IMPORT door, so a locally minted composite is never priced at all"): THE ONE STATEMENT of the mint's admission inequality -- cost = base_cost + mint.EXTENT_RATE * extent (base_cost defaults to effects.encoding_cost_atom on `changed`; the atom door passes the atom's own encoding cost, byte-identical), R = mint.RESIDUAL_CELL_COST * changed + mint.UNEXPLAINED_PREMIUM, admit iff cost < R and cost < mint.MDL_MARGIN * R; constants the MINT'S, imported lazily. CONSUMERS: consumer.admission_price (both branches -- the import door; no inequality of its own remains) and composer._attempt (this receipt: the LOCAL MINT pays it on the derived csig price BEFORE Gamma.compose; a refused chain states price-refused and the price, a csig-less chain csig-underivable). The stage-1 "third consumer, the gate's PAY" made real at the composer. Gate tests/gate/test_composer_stage45.py S2 (a dear chain refused + the door's identical verdict; structural: MDL_MARGIN consulted in exactly this function). |

## INSTRUMENTS (rung-0 reads; invoked by the BEAT PROTOCOL, never by the live path)

An instrument's receipt is not a live-path call site — it is a NAMED CONSUMER in
the read protocol (THE_LADDER.md: "every produced stream needs a named consumer
INCLUDING THE SEAT'S OWN SOURCES — the beat protocol is the stream's consumer, by
name"). The gate's status vocabulary has three words and none of them is
INSTRUMENT, so these rows carry the status the gate can CHECK — SEVERED, i.e. no
production module references the symbol — with `[instrument]` naming the reason
(the same idiom as `marketplace-iced`: severed BY DESIGN, stated not hidden).
That is not a workaround, it is the right guard here: for the efficiency read a
production reference would BE the prohibited state (a rung-0 read becoming a
knob inside the loop), so the inverse-reference assert going red is exactly the
alarm the ITEM-2 QUALIFICATION asks for. Registry vocabulary gap recorded under
THE_LADDER's INEXPRESSIBLE-STATE GENUS; expressing it is the maintainer's call,
not this build's.

| name | symbol | site | status | date | note |
|------|--------|------|--------|------|------|
| efficiency-read | tools.efficiency_read:efficiency_report | tools/efficiency_read.py:310 | SEVERED | 2026-08-17 | [instrument] ITEM 2 (THE_GOALS.md ITEM-2 QUALIFICATION): rung-0 RESOLUTION read — distance-to-that-player per game+level, reference read off disk from replay metadata baseline_actions, observed = MIN over banked winning_sequences/archived_sequences, tail-scoped. Registered READ, NEVER TARGET: no knob, no arm objective, until levels move — a production reference to this symbol IS the prohibited state and this row's inverse-reference assert is the alarm. Every output line carries "one reference run, not a reference distribution" (EXECUTION RIDER 2). Invocation site = the beat protocol (THE_LADDER.md rung 0, levels_completed); output feeds beat reports only. Read-only on every book. Gate tests/gate/test_efficiency_read.py |
| context-minimiser | tools.context_minimiser:minimise_stream | tools/context_minimiser.py:99 | SEVERED | 2026-08-20 | [instrument] W2 STAGE 2 part 3 (PREREG_W2_STAGE2_CONTEXT_MIN.md): the RETRO context-minimisation pass -- A TOOL, NOT AUTO-RUN, operator-invoked. Reads a box's atoms stream READ-ONLY, groups by key, minimises multi-observation keys by the same intersection the mint applies (changed+ring retained, context_full preserved), emits singletons unchanged marked singleton:true, writes to a NEW output stream only (refuses in-place). F1 enforced INSIDE the pass: a minimised atom that fails to predict any of its own recorded observations is emitted unminimised, marked reverted:true. EVIDENCE LIMIT stated in its docstring, not papered over (measured 2026-08-20: 2,009 records, 1,967 keys, 42 keys with 2+ same-shape observations, ALL byte-identical contexts -- 41/42 import-echoes -- so the pass shrinks nothing on today's streams; the mechanism ships ahead of the evidence it needs). A production reference to this symbol would be the pass running itself -- the inverse-reference assert is the alarm. Gate tests/gate/test_context_min.py |
| action-book | engines.egocentric.action_book:load_action_book | engines/egocentric/action_book.py:58 | SEVERED | 2026-08-20 | [instrument] STAGE 0 of the reasoning gate (record/findings/PROPOSAL_REASONING_GATE.md §3): THE ACTION BOOK -- per (game, action) semantics DERIVED entirely from the agent's own recorded history: the Γ atoms stream (action int, changed-cell counts, ttypes, sigma colour deltas, TRANSLATE dx/dy) + the action_traces DB READ-ONLY (per-action frequency, frame-change rate, score-change distribution, and COST evidence from the budget_total/budget_spend columns, produced-and-unread before this build), plus INVERSE PAIRINGS evidence-counted from both sources (trace steps f0-a->f1-b->f0 with f1 != f0 and the chain verified; typed atom deltas composed to identity through effects.invert_transform -- the world's own algebra, authored nowhere). Authored action semantics would be checking-against-us; derived ones are the agent's history made citable (gated structurally AND by permutation equivariance). ARTIFACT: .runs/swarm/<box>/ego_fabric/collective/action_book.json, REBUILT FROM SCRATCH each run by tools/build_action_book.py (header carries the source stream positions; derived state, never the sole holder of anything; sources never written). HONESTY: every claim carries its evidence n; absence is an explicit null with a NAMED reason; inverse_of reports n VERBATIM (n=1 never promoted). Read surface: load_action_book / effect_summary / inverse_of / cost_of, one in-memory dict. NOTHING IN THE LIVE PATH CONSULTS IT YET -- a stated promise, same discipline as context-minimiser: the named consumer is the STAGE-1 SHADOW GATE (its ledger reads ARE the consumption wires). A production reference to this symbol before that build lands is the alarm -- the inverse-reference assert. Gate tests/gate/test_action_book.py |
| regen-series | tools.regen_series:regen_snapshot | tools/regen_series.py:218 | SEVERED | 2026-08-17 | [instrument] ITEM 3 (THE_LADDER.md THE REPORT FORMAT rider 2, "THE RATIO HAS ONE TERM"): appends one per-fabric SELF-rho snapshot per invocation to .runs/regen_series.jsonl — drift of fabric-now vs fabric-at-its-last-snapshot at rho.rho_at rungs 0/1/2, never averaged. Named consumer = the roving-pool gate pricing, whose second term (regeneration rate) is uncollectable without this series; adoption of that gate is still zero, so it ships as a BEAT-READ COMPONENT and the series accumulates meanwhile. Bounded (max_fabrics/max_atoms/max_digest, truncation flagged), read-only on fabrics, append-only + torn-tail-safe on the series, NO wall clock in the record (stamp passed in — a clock index would be a latent, EVICTION SPEC RIDER 1). Invocation site = the beat protocol (THE_LADDER.md rung 0b/beat read). Gate tests/gate/test_regen_series.py |

## SEVERED ORGANS (the sweep's eleven, + minor, + build-scan finds)

| name | symbol | site | status | date | note |
|------|--------|------|--------|------|------|
| role-multiplier | cognitive_game_player:CognitiveGamePlayer._role_allowance_multiplier | cognitive_game_player.py:245 | SEVERED | 2026-08-16 | S1 [referenced-but-dead] fed None forever — no agent role reaches the call; every purse ×1.0 (known, unfixed) (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_game_player.py:240) |
| bracket-rt-print | tools.bracket_rt:live_report | tools/bracket_rt.py:484 | SEVERED | 2026-08-16 | S2 R_T=0.000 is a PRINTED CONSTANT on a key-to-itself join (tools/bracket_rt.py:484-490), not arithmetic |
| agent-motion | engines.egocentric.bank:AGENT_MOTION_ENABLED | engines/egocentric/bank.py:209 | SEVERED | 2026-08-16 | S3 family never fed — animacy never runs live; flag True, branch dead (bank.py:209-264) |
| conditional-miner | engines.egocentric.effects:ConditionalMiner | cognitive_loop.py:1723 | SEVERED | 2026-08-16 | S4 live router constructed WITHOUT miner= → early-returns; conditional_atoms never populated; triage NEAR FRONT (RECEIPT REFRESHED 2026-08-18 by the LINK3 hook build, LOUDLY: this row was CLEAN at ad26e69 and was rotted BY THIS BUILD -- _goal_bank and CognitiveLoop.bank_replay_levelup were inserted above it, moving the site further than the +/-30 drift tolerance; was cognitive_loop.py:1657) |
| class-fission | engines.egocentric.bank:ClassFissionSocket | engines/egocentric/bank.py:285 | SEVERED | 2026-08-16 | S5 zero production callers of note_outcome/resolve; RoleBinder.on_fission never called live; triage NEAR FRONT |
| mute-probe | engines.egocentric.verdicts:MuteHandler.mute | cognitive_loop.py:1885 | SEVERED | 2026-08-16 | S6 [referenced-but-dead] observed_counts hardcoded all-zeros at the call; probe only PRINTED; release() never called — steers nothing (receipt refreshed 2026-08-18: the LINK3 hook build inserted _goal_bank + CognitiveLoop.bank_replay_levelup above this site; was cognitive_loop.py:1973)  (RECEIPT REFRESHED 2026-08-20 by the W1 narration build, LOUDLY: bet-before-act insertions moved sites; was :2039) |
| fabric-janitor | engines.egocentric.janitor:FabricJanitor | engines/egocentric/janitor.py:60 | SEVERED | 2026-08-16 | S7 NEVER CONSTRUCTED in production — compaction has never run (explains 268k-line queues) |
| broken-rebinding | engines.egocentric.router:BROKEN_REBINDING | engines/egocentric/router.py:71 | SEVERED | 2026-08-16 | S9 bin unreachable — binding_stale never produced, refit_queue never drained |
| symbolic-gameplay | engines.reasoning.symbolic_reasoning_engine:SymbolicGameplayIntegration | engines/reasoning/symbolic_reasoning_engine.py:2274 | SEVERED | 2026-08-16 | S10 never constructed in production; triage DELETE (dead weight) |
| falsified-ledger | engines.egocentric.falsified_ledger:FalsifiedLedger | engines/egocentric/falsified_ledger.py:63 | SEVERED | 2026-08-16 | S11 never constructed; the __init__ re-export hid it from vulture; triage DELETE (dead weight) |
| falsified-ledger-entry | engines.egocentric.falsified_ledger:_Entry | engines/egocentric/falsified_ledger.py:56 | SEVERED | 2026-08-16 | helper of S11 — dies with its ledger |
| mint-ep-kwarg | engines.egocentric.mint:MDLMint.consider | engines/egocentric/mint.py:329 | SEVERED | 2026-08-16 | minor [referenced-but-dead] the ep= kwarg is never passed by any caller (internal counter used everywhere); consider() itself is the LIVE mint-core wire. 2026-08-16: bump_episode() now fires once per boundary at the end_game settle (cognitive_loop.py:640) so the INTERNAL ordinal is retry-precise — the kwarg stays unpassed (SITE REFRESHED 2026-08-20 by the W2 STAGE 2 CONTEXT-MIN build (PREREG_W2_STAGE2_CONTEXT_MIN.md), LOUDLY: rotted BY THIS BUILD -- the EXTENT_RATE constant block + module-docstring additions were inserted above consider(); was mint.py:191) (SITE REFRESHED 2026-08-20 by THE VERDICT REASON FIELD build (W4; gate tests/gate/test_verdict_reason.py), LOUDLY: rotted BY THIS BUILD -- the REASON_* vocabulary constants, the module-docstring reason paragraph and the _record/_mdl_failed additions were inserted above consider(), which now sits at mint.py:329; was mint.py:229, and NOTED for honesty: the RE-POINT AMENDMENT build had already drifted consider() to mint.py:261 without refreshing this SEVERED row -- this refresh absorbs both moves) |
| marketplace-iced | engines.egocentric.pricing:Marketplace | engines/egocentric/pricing.py:65 | SEVERED | 2026-08-16 | iced BY DESIGN (C33 step 1: bets drive nothing); informative_salience is the live half (pricing-salience row) |
| pricing-hypothesis | engines.egocentric.pricing:Hypothesis | engines/egocentric/pricing.py:33 | SEVERED | 2026-08-16 | helper of the iced marketplace |
| pricing-resolution | engines.egocentric.pricing:Resolution | engines/egocentric/pricing.py:59 | SEVERED | 2026-08-16 | helper of the iced marketplace |

## SEQUENCE_MINER — THE RE-EXPORT BLIND SPOT, SECOND INSTANCE (2026-08-17, Seat 2)
STATUS: SEVERED. engines/planning/sequence_miner.py, 26,790 bytes.
Receipt: engines/planning/__init__.py:5 re-exports SequenceMiner; NO OTHER REFERENCE
EXISTS IN THE REPOSITORY. `SequenceMiner(` appears ZERO times — production AND tests.
WHAT IT CONTAINS: compute_level_breakpoints() (:76), backfill_level_breakpoints()
(:143-181), MiningResult.level_breakpoints_computed (:50), and the schema column
level_breakpoints TEXT (complete_database_schema.sql:5363).
LIVE DATA: winning_sequences with a populated level_breakpoints = 0, in EVERY box
checked (ar25 3 sequences, cd82 5, bp35 0). The field has never been written.
WHY THIS ROW MATTERS MORE THAN THE OTHER 23 SEVERED ROWS: this is the organ that
would have prevented the replay defect. The distinction between "these actions solved
THIS board" and "these actions reach where I was" was ANTICIPATED, IMPLEMENTED IN FULL,
RE-EXPORTED, AND NEVER CALLED ONCE. It is not schema-as-intention; it is
MACHINERY-as-intention.
AND IT IS THE SAME TRAP TWICE: the wiring registry gate was written specifically to
close the __init__ re-export blind spot ("the re-export hid it from vulture", feb7e26).
This organ sits behind that exact blind spot and was not caught, because the registry
covers engines/egocentric/ and sequence_miner.py lives in engines/planning/.
THE SCOPE RATIO IS NOT AN ABSTRACTION — IT IS WHY THIS ONE WAS MISSED.

## U-2 — FRONTIER-CHECKPOINT RETENTION RE-ROUTED (2026-08-21, Seat 3 build)
disk rulings 2026-08: score-keyed deletion removed; ground evidence protected.
`safe_cleanup.py::_clean_frontier_checkpoints` (was safe_cleanup.py:2060-2149; D-1 in
record/findings/RETENTION_POLICY.md, read in record/findings/F8A_READ.md as LATENT NOT
INERT) no longer keeps top-20 per (game_type, level_number) by `survival_score DESC`
with a hard DELETE. It now ARCHIVE-THEN-TRUNCATEs: every row is copied verbatim into
`frontier_checkpoints_archive` (same columns + archived_at; append-only, never itself a
cleanup target), the copy is VERIFIED BY COUNT BEFORE THE WIPE, then the live table is
truncated with NO predicate — no row is ever SELECTED for removal, so no proxy can be
the key. Gate: tests/gate/test_u2_checkpoint_retention.py (behavioural full-archive +
structural no-score-comparison AST/source scan + append-only across sweeps).
RECEIPT-ROT CHECK, STATED LOUDLY: NO registry row cites safe_cleanup.py — the
frontier-named rows (plan-veto, harvest, movement-bias) all point at
engines/egocentric/frontier.py, which this build did not touch — so no receipt moved
and none needed refreshing. The four stale tests in tests/test_safe_cleanup.py that
asserted the PRE-RULING spec (delete zero-score game_results) were ALIGNED the same
day to guard the ruled behaviour instead; they go red if score-keyed deletion returns.
