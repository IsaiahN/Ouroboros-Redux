# ROLE COLLAPSE AUDIT (2026-08-16, read-only; receipts in task record)
BREAK DATED: f673b9a (2026-08-13) — OUR supervisor made 25 boxes of population 6.
RF1 STARVED (primary): int truncation — int(6*0.15)=0 optimizers, int(6*0.05)=0
exploiters; every fallback gates on assigned<target; 0<0 never true. UNREACHABLE BY
CONSTRUCTION at pop 6. RF2 SEMANTIC-FALLACY: "emergent" roles from schema DEFAULT 0.5
-> every fresh agent lands generalist band; deterministic 4G+2P per box — the observed
collapse is the code's correct output (RF8). RF3 phantom mode memory (effectiveness
updater has ZERO callers; gen-1 mix ratchets permanent). RF4 lifecycle < every
adaptive loop (2h recycle + fresh populations; self-determination channel dead — no
callers). RF5 THE KILLER: G17 RUNS ON ZERO CLASSES — player reads agent.role which
NOTHING SETS; assignments write agents.specialization which the player NEVER READS;
everyone gets 1.0x. The committed role economy was never live in the swarm. The gate
test asserted table values, never that a role reaches the purse. AdaptiveActionLimits
(the full salary organ) is orphaned. RF6 the mixer's ledger (agent_operating_modes) is
in MY supervisor's TELEMETRY_TABLES — knowledge classified as trash, one table one
deletion policy. RF7 FIVE competing canonical target tables (60/10/20/10 vs 60/15/20/5
vs 10/60/30(+stale) vs 60/30/10/5 vs 60/25/10/5; one queries a nonexistent column).
RESTORATION SKETCH (not built): mix -> Register O as COUNTS (largest-remainder; small-N
can never zero a class; env-injected like LP_DRIVE_ARM); maturity -> measured latent
(banked-sequence coverage per game_type, never per-box win_detected); shift schedule ->
arm-tested G knob; purse values stay G* (self-tuning-excluded); role -> AgentState
field feeding purse + kin_key only; no game-id keys (OOD law); single target table;
wire-or-delete the dead organs; split knowledge/telemetry rows before GC.
COMBINED PROGRAM: goal side F1-F8 (PIPELINE_AUDIT) + role side RF1-RF7 -> preregs with
falsifiers + control-arm verdicts; awaiting maintainer's word to spec the wave.
