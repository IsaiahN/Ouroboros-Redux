# VICTORY PROTOCOL (Isaiah, 2026-08-14) — on 25/25 FULL WINS (all levels, every public game)
1. NOTIFY ISAIAH IMMEDIATELY. 2. STOP all game-playing (kill swarm). 3. Timestamped
commit. 4. COPY the data (.runs fabrics+DBs -> dated archive dir). 5. Save + annotate
the atoms (census: per-atom source_game/provenance, per-game levels). 6. Notify again
with the archive location.
RECORDS KEPT NOW (cost nothing, unrecoverable later): frame snapshots on LEVEL
INCREMENTS; source_game per atom (native + imported provenance); the FULL settlement
stream (janitor must ARCHIVE folded records, never discard).
GAPS FOUND ON RECEIPT (fix dispatched): (a) level-up pre/post frames extracted for
predicates but NOT persisted; (b) janitor folds settlements beyond 100 into counters —
history lost — must archive to a side file first.
